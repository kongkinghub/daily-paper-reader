---
title: "RePO: Bridging On-Policy Learning and Off-Policy Knowledge through Rephrasing Policy Optimization"
title_zh: RePO：通过改写策略优化桥接同策略学习与异策略知识
authors: "Linxuan Xia, Xiaolong Yang, Yongyuan Chen, Enyue Zhao, Deng Cai, Yasheng Wang, Boxi Wu"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10819v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 桥接在线策略学习与离线知识吸收
tldr: 针对大语言模型在特定领域对齐时，SFT易损害泛化性而强化学习（RL）难以处理困难样本的问题，本文提出RePO。该方法通过让模型重述离策（off-policy）知识，将其转化为符合自身分布的高质量轨迹，并动态替换低奖励样本。RePO结合了离策知识的高效利用与在策（on-policy）学习的稳定性，在多个基准测试中达到了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在解决在策强化学习难以吸收困难样本，以及离策强化学习因分布偏移导致训练不稳定的矛盾。
method: 提出重述策略优化（RePO），通过让模型将离策知识重述为符合自身分布的轨迹，并动态替换低奖励的采样结果。
result: 在多个基准测试中，RePO显著提升了对困难样本的利用率，并超越了现有基线模型，取得领先性能。
conclusion: RePO成功弥合了在策学习与离策知识之间的鸿沟，为大语言模型的高效、稳定对齐提供了新方案。
---

## 摘要
在特定领域数据上对齐大语言模型（LLMs）仍然是一个根本性的挑战。有监督微调（SFT）提供了一种注入领域知识的直接方法，但往往会降低模型的泛化性。相比之下，同策略（on-policy）强化学习（RL）保留了泛化性，但无法有效吸收超出模型当前推理水平的困难样本。最近的异策略（off-policy）强化学习尝试提高了困难样本的利用率，但由于强制向异策略知识进行分布偏移，导致了严重的训练不稳定性。为了调和有效的异策略知识吸收与同策略强化学习的稳定性，我们提出了改写策略优化（Rephrasing Policy Optimization, RePO）。在 RePO 中，策略模型被提示首先理解异策略知识，然后将其改写为符合其自身风格和参数分布的轨迹。RePO 动态地将低奖励的采样轨迹替换为这些改写后的高质量轨迹。该策略引导模型走向正确的推理路径，同时严格保持同策略训练动力学。在多个基准测试上的实验表明，RePO 提高了困难样本的利用率，并优于现有基准，达到了最先进的性能。

## Abstract
Aligning large language models (LLMs) on domain-specific data remains a fundamental challenge. Supervised fine-tuning (SFT) offers a straightforward way to inject domain knowledge but often degrades the model's generality. In contrast, on-policy reinforcement learning (RL) preserves generality but fails to effectively assimilate hard samples that exceed the model's current reasoning level. Recent off-policy RL attempts improve hard sample utilization, yet they suffer from severe training instability due to the forced distribution shift toward off-policy knowledge. To reconcile effective off-policy knowledge absorption with the stability of on-policy RL, we propose Rephrasing Policy Optimization (RePO). In RePO, the policy model is prompted to first comprehend off-policy knowledge and then rephrase it into trajectories that conform to its own stylistic and parametric distribution. RePO dynamically replaces low-reward rollouts with these rephrased, high-quality trajectories. This strategy guides the model toward correct reasoning paths while strictly preserving on-policy training dynamics. Experiments on several benchmarks demonstrate that RePO improves hard-sample utilization and outperforms existing baselines, achieving state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《RePO: Bridging On-Policy Learning and Off-Policy Knowledge through Rephrasing Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了大语言模型（LLM）在特定领域（如数学、医疗、法律）对齐时的核心矛盾：
*   **SFT（有监督微调）的局限**：虽然能直接注入知识，但由于是“异策略（Off-policy）”训练，模型倾向于机械模仿静态数据，容易导致泛化能力下降和灾难性遗忘。
*   **On-policy RL（同策略强化学习，如 GRPO）的局限**：虽然能保持泛化性，但对于模型当前能力无法解决的“困难样本”，由于采样不到正确的推理路径，模型无法获得有效奖励，导致学习陷入停滞。
*   **现有 Off-policy RL 的缺陷**：如 LUFFY 等方法尝试直接引入外部专家轨迹，但由于外部模型与当前模型之间存在**词表不匹配**和**分布偏移**，会导致训练极度不稳定（梯度爆炸或模型崩溃）。

**研究动机**：如何既能利用高质量的外部专家知识解决困难样本，又能保持同策略强化学习的训练稳定性。

### 2. 方法论：改写策略优化（RePO）
RePO 的核心思想是让模型在学习外部知识前先进行“消化”和“内化”。

*   **核心思想**：通过“理解并改写”机制，将外部异策略知识转化为符合模型自身风格和参数分布的“伪同策略”轨迹。
*   **关键技术细节**：
    1.  **知识内化（改写采样）**：给定一个困难问题的专家解法（Off-policy Knowledge），使用特定的 Prompt 引导模型先理解该逻辑，然后用模型自己的“语气”和“逻辑”重新表述一遍。
    2.  **动态注入策略**：在 RL 训练循环中，模型对每个问题生成一组（Group）轨迹。RePO 会监控这组轨迹的失败率。如果失败率超过阈值（说明模型独立探索失败），则用预先生成的“改写轨迹”替换掉组内奖励最低的样本。
    3.  **优化目标**：基于 GRPO 框架，利用组内相对奖励计算优势函数（Advantage），并进行策略更新。
*   **稳定性原理**：由于改写后的轨迹是由模型自身生成的，其 Token 序列符合模型的局部概率分布，避免了直接引入外部 Token 导致的 Logits 剧烈波动，从而确保了梯度平滑。

### 3. 实验设计
*   **数据集**：
    *   **SuperGPQA**：涵盖 285 个学科的 11k 个研究生水平非计算问题。
    *   **OpenR1-Math**：包含 Hard 子集（18k 样本）和 Multi-source Harder 子集（10k 样本，由 DeepSeek-R1/V3、GPT 等多模型蒸馏）。
    *   **金融领域数据**：10k 个金融推理样本。
*   **Benchmark（基准测试）**：
    *   数学：AIME 24/25, AMC, MATH-500, Minerva, OlympiadBench。
    *   通用/知识：GPQA, ARC-c, MMLU-Pro。
    *   金融：FinanceIQ, FinEval, FinLLM Eval 等 5 个专业榜单。
*   **对比方法**：Qwen 系列基座模型、GRPO（纯同策略 RL）、LUFFY（带正则化的异策略 RL）、SFT（有监督微调）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号（如 H100 或 A100）及具体数量。
*   **训练配置**：提到了学习率为 1e-6，Batch Size 为 512，每个 Prompt 生成 8 个 Rollout。SFT 阶段最大序列长度为 16k。
*   **框架**：RePO 基于 `verl` 框架实现，LUFFY 使用其官方开源代码。

### 5. 实验数量与充分性
*   **实验规模**：论文在通用知识、数学推理、金融垂直领域三个维度进行了大规模实验，涵盖了从 4B 到 8B 规模的模型。
*   **充分性分析**：
    *   **多维度验证**：不仅对比了最终性能，还深入分析了训练过程中的熵（Entropy）、梯度范数（GradNorm）和奖励曲线，证明了稳定性。
    *   **极端压力测试**：在“Hard”和“Multi-source”子集上的实验非常具有说服力，展示了在基准方法（LUFFY）崩溃的情况下 RePO 依然稳健。
    *   **公平性**：所有对比方法在相同的数据子集和样本量下进行，具有较高的客观性。

### 6. 主要结论与发现
*   **稳定性突破**：RePO 解决了异策略知识引入时的训练崩溃问题。实验显示 LUFFY 在处理极难样本时梯度爆炸，而 RePO 的梯度范数保持平稳。
*   **性能领先**：在 GPQA 和 AIME 等高难度任务上，RePO 显著优于 GRPO 和 SFT。
*   **知识与推理的平衡**：在金融领域实验中，SFT 虽然能注入知识但会导致数学推理能力减半，而 RePO 在大幅提升金融知识水平的同时，较好地保留了模型的原始推理能力。
*   **多源数据鲁棒性**：RePO 能有效处理来自不同专家模型（如 GPT 和 DeepSeek）的混合数据，表现出极强的兼容性。

### 7. 优点
*   **机制巧妙**：通过“改写”这一动作，将复杂的分布对齐问题简化为模型自发的语言生成问题。
*   **即插即用**：可以无缝集成到现有的组优化 RL 框架（如 GRPO）中。
*   **解决冷启动**：为 RL 无法通过自我探索解决的极难问题提供了稳定的引导路径。

### 8. 不足与局限
*   **推理开销**：在训练前或训练中需要额外的改写步骤，增加了数据准备的计算成本。
*   **依赖专家质量**：如果外部提供的专家轨迹本身有误，模型改写后的轨迹也会误导训练（尽管有验证器过滤，但仍受限于专家源）。
*   **改写能力边界**：如果问题极度困难，超出了训练模型基本的“阅读理解”能力，模型可能无法准确改写专家逻辑，导致引导信号失效。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
