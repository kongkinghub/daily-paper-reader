---
title: "A Step Back: Prefix Importance Ratio Stabilizes Policy Optimization"
title_zh: 退后一步：前缀重要性比率稳定策略优化
authors: "Shiye Lei, Zhihao Cheng, Dacheng Tao"
date: 2026-01-30
pdf: "https://arxiv.org/pdf/2601.22718v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: 离线强化学习算法
tldr: 针对大语言模型强化学习后训练中离策（off-policy）训练不稳定的问题，本文指出理论上正确的修正项应为前缀重要性采样比率，而非通用的逐标记比率。作者提出了MinPRO算法，通过使用前缀中最小标记比率作为非累积替代方案，有效缓解了训练波动。在数学推理任务上的实验证明，MinPRO在处理大规模离策偏移时具有更强的稳定性和更高的性能峰值。
selection_source: fresh_fetch
motivation: 现有的逐标记重要性采样在离策程度较大时会导致LLM强化学习训练动态不稳定。
method: 提出MinPRO目标函数，利用前缀中最小标记比率来替代不稳定的累积前缀比率，以实现更稳健的策略优化。
result: 在多种数学推理基准测试和不同架构的LLM上，MinPRO显著提升了离策环境下的训练稳定性和最终性能。
conclusion: 重新审视前缀重要性比率对于稳定LLM离策策略优化至关重要，MinPRO为该问题提供了一个简单且有效的解决方案。
---

## 摘要
强化学习（RL）后训练已日益展现出激发大语言模型（LLM）推理能力的强大潜力。为了提高训练效率，通常采用离策（off-policy）方式，利用旧的采样策略生成采样序列（rollouts），并以此更新当前的目标策略。为了修正采样策略与目标策略之间的偏差，大多数现有的 RL 目标函数依赖于 Token 级的重要性采样比率，这主要是由于其计算简便且具有数值稳定性。然而，我们观察到，当离策程度较大时，Token 级的修正往往会导致训练动态不稳定。在本文中，我们重新审视了离策条件下的 LLM 策略优化，并证明理论上严谨的修正项应该是前缀重要性比率（prefix importance ratio），而将其放宽为 Token 级的近似可能会导致 RL 后训练的不稳定性。为了在存在较大离策偏移的情况下稳定 LLM 优化，我们提出了一种简单而有效的目标函数：最小前缀比率（MinPRO）。MinPRO 使用基于前序前缀中观察到的最小 Token 级比率的非累积代理项，取代了不稳定的累积前缀比率。在稠密模型和混合专家（MoE）LLM 上进行的广泛实验，以及在多个数学推理基准测试中的表现，证明了 MinPRO 在离策机制下显著提高了训练稳定性和峰值性能。

## Abstract
Reinforcement learning (RL) post-training has increasingly demonstrated strong ability to elicit reasoning behaviors in large language models (LLMs). For training efficiency, rollouts are typically generated in an off-policy manner using an older sampling policy and then used to update the current target policy. To correct the resulting discrepancy between the sampling and target policies, most existing RL objectives rely on a token-level importance sampling ratio, primarily due to its computational simplicity and numerical stability. However, we observe that token-level correction often leads to unstable training dynamics when the degree of off-policyness is large. In this paper, we revisit LLM policy optimization under off-policy conditions and show that the theoretically rigorous correction term is the prefix importance ratio, and that relaxing it to a token-level approximation can induce instability in RL post-training. To stabilize LLM optimization under large off-policy drift, we propose a simple yet effective objective, Minimum Prefix Ratio (MinPRO). MinPRO replaces the unstable cumulative prefix ratio with a non-cumulative surrogate based on the minimum token-level ratio observed in the preceding prefix. Extensive experiments on both dense and mixture-of-experts LLMs, across multiple mathematical reasoning benchmarks, demonstrate that MinPRO substantially improves training stability and peak performance in off-policy regimes.

---

## 论文详细总结（自动生成）

以下是对论文《A Step Back: Prefix Importance Ratio Stabilizes Policy Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：在大语言模型（LLM）的强化学习（RL）后训练中，为了提高系统效率，通常采用**离策（Off-policy）**工作流，即利用旧策略生成的采样数据来更新当前新策略。
*   **核心问题**：现有的 RL 目标函数（如 PPO、GRPO）大多采用 **Token 级重要性采样比率**。作者发现，当离策程度较大（采样策略与目标策略偏差大）时，这种 Token 级的近似会导致训练动态极不稳定，甚至引发训练崩溃。
*   **研究动机**：重新审视策略梯度理论，指出理论上严谨的修正项应是**前缀重要性比率（Prefix Importance Ratio）**，并寻求一种既符合理论又具备数值稳定性的实现方案。

### 2. 方法论：MinPRO 算法
*   **核心思想**：回归理论，使用前缀信息修正分布偏移。由于完整的前缀比率（Token 比率的累积乘积）在长序列中会产生数值爆炸和长度偏差，作者提出了一种非累积的代理指标。
*   **关键技术细节**：
    *   **理论推导**：证明了在离策条件下，策略梯度应乘以从序列起始到当前位置 $t$ 的累积比率 $\rho_{1:t}$。
    *   **MinPRO 定义**：引入“最小前缀比率” $\underline{\rho}_t = \min_{i < t} \rho_i$，即当前 Token 之前所有位置中最小的 Token 比率。
    *   **目标函数**：将不稳定的累积乘积 $\rho_{1:t}$ 替换为 $\underline{\rho}_t \cdot \rho_t$。
*   **算法逻辑**：当某个前缀片段在当前策略下出现的概率极低时（$\underline{\rho}_t$ 极小），MinPRO 会自动抑制该 Token 产生的梯度贡献。这种机制能有效防止极少数高比率 Token 主导梯度更新，从而稳定训练。

### 3. 实验设计
*   **数据集**：使用 DAPO-Math-17K 进行强化学习训练。
*   **评测基准（Benchmarks）**：涵盖 7 个数学推理任务，包括 AMC23、AIME24、AIME25、MATH500、Olympiad、Minerva 和 GSM8K。
*   **对比方法**：
    *   **GRPO**：主流的硬截断（Hard-clipping）方法。
    *   **GSPO**：使用全序列重要性比率的方法。
    *   **CISPO**：采用软截断（Soft-clipping）的方法。
    *   **M2PO**：专门针对大离策偏移设计的改进型硬截断方法。
*   **实验场景**：通过设置训练延迟（Data Staleness $n=2$）来模拟高离策环境，验证算法在极端不稳定性下的表现。

### 4. 资源与算力
*   **训练框架**：使用 VeRL 框架，推理引擎为 vLLM。
*   **并行策略**：Qwen3-8B/14B 使用 PyTorch FSDP；Qwen3-30B-A3B (MoE) 使用 Megatron-LM。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但提到了针对不同规模模型采用了不同的分布式训练配置以优化内存。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了三种不同架构和规模的模型：稠密模型（8B、14B）和混合专家模型（30B MoE）。
    *   在 7 个不同的数学基准上进行了 Pass@1 和 Pass@k 的全面评估。
*   **充分性评价**：实验设计较为充分。作者不仅对比了最终性能，还详细展示了训练过程中的**奖励曲线（Reward Curves）**和**熵变化曲线**，直观证明了稳定性。通过引入高离策偏移的特殊设置，客观地验证了 MinPRO 在极端条件下的鲁棒性。

### 6. 主要结论与发现
*   **稳定性显著提升**：在离策环境下，GRPO 和 CISPO 等传统方法表现出剧烈的奖励波动或训练崩溃，而 MinPRO 保持了平稳上升的奖励曲线。
*   **性能突破**：MinPRO 在所有测试模型上均取得了最高的平均 Pass@1 分数，优于现有的强基线（如 M2PO）。
*   **可扩展性**：证明了该方法在 MoE 架构和更大参数规模（30B）模型上的有效性。

### 7. 优点
*   **理论驱动**：并非纯启发式改进，而是基于策略梯度定理的重新发现。
*   **实现简单**：MinPRO 仅需在现有目标函数中增加一个 `min` 操作，不增加计算负担，且无需额外的 Critic 网络。
*   **鲁棒性强**：有效解决了长序列 LLM 在异步/离策训练中的崩溃问题，对工程实践具有高度指导意义。

### 8. 不足与局限
*   **任务领域单一**：实验完全集中在数学推理任务上，尚未在代码生成、通用对话或指令遵循等其他长序列任务中验证。
*   **算力信息不透明**：缺乏具体的硬件消耗和训练时长数据，不便于其他研究者评估复现成本。
*   **超参数依赖**：虽然解决了比率爆炸问题，但仍依赖于截断超参数（$\epsilon$）的设置，文中未深入探讨其在不同任务下的敏感度。

（完）
