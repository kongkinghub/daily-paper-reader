---
title: "OffSeeker: Online Reinforcement Learning Is Not All You Need for Deep Research Agents"
title_zh: OffSeeker：深度研究智能体并非仅需在线强化学习
authors: "Yuhang Zhou, Kai Zheng, Qiguang Chen, Mengkang Hu, Qingfeng Sun, Can Xu, Jingjing Chen"
date: 2026-01-26
pdf: "https://arxiv.org/pdf/2601.18467v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: 研究智能体的离线训练以替代昂贵的在线强化学习
tldr: 本研究针对深度研究智能体过度依赖高成本在线强化学习（RL）的问题，提出了全开源的离线训练方案OffSeeker。通过引入DeepForge任务合成框架，研究者生成了大规模研究查询，并构建了包含66k问答、33k SFT轨迹及21k DPO对的高质量数据集。实验证明，仅通过离线训练的8B模型OffSeeker在多个基准测试中不仅优于同尺寸模型，甚至能媲美重度在线RL训练的30B模型，为高效构建研究智能体提供了新路径。
selection_source: fresh_fetch
motivation: 旨在解决深度研究智能体在在线强化学习中面临的高昂API成本以及高质量离线训练轨迹稀缺的挑战。
method: 开发了DeepForge任务合成框架并构建大规模离线数据集，通过SFT和DPO训练出完全离线的OffSeeker模型。
result: OffSeeker (8B) 在六项基准测试中表现优异，不仅领先同类模型，还达到了与重度在线RL训练的30B模型相当的水平。
conclusion: 研究证明了高质量离线训练是构建强大研究智能体的可行且高效的替代方案，无需依赖昂贵的在线强化学习。
---

## 摘要
深度研究智能体在处理长程任务方面展现出巨大潜力。然而，目前的最优性能通常依赖于在线强化学习（RL），由于大量的 API 调用，其经济成本十分高昂。虽然离线训练提供了一种更高效的替代方案，但其进展受限于高质量研究轨迹的稀缺。在本文中，我们证明了构建强大的研究智能体并非一定需要昂贵的在线强化学习。为了弥补这一差距，我们推出了一套专为有效离线训练设计的全开源套件。我们的核心贡献包括：DeepForge，一个开箱即用的任务合成框架，无需繁重的预处理即可生成大规模研究查询；以及精选的 6.6 万个问答对、3.3 万条 SFT 轨迹和 2.1 万个 DPO 对。利用这些资源，我们训练了 OffSeeker (8B)，这是一个完全通过离线方式开发出的模型。在六个基准测试上的广泛评估表明，OffSeeker 不仅在同等规模的智能体中处于领先地位，而且与通过重度在线强化学习训练的 30B 参数系统相比也具有竞争力。

## Abstract
Deep research agents have shown remarkable potential in handling long-horizon tasks. However, state-of-the-art performance typically relies on online reinforcement learning (RL), which is financially expensive due to extensive API calls. While offline training offers a more efficient alternative, its progress is hindered by the scarcity of high-quality research trajectories. In this paper, we demonstrate that expensive online reinforcement learning is not all you need to build powerful research agents. To bridge this gap, we introduce a fully open-source suite designed for effective offline training. Our core contributions include DeepForge, a ready-to-use task synthesis framework that generates large-scale research queries without heavy preprocessing; and a curated collection of 66k QA pairs, 33k SFT trajectories, and 21k DPO pairs. Leveraging these resources, we train OffSeeker (8B), a model developed entirely offline. Extensive evaluations across six benchmarks show that OffSeeker not only leads among similar-sized agents but also remains competitive with 30B-parameter systems trained via heavy online RL.

---

## 论文详细总结（自动生成）

以下是对论文《OffSeeker: Online Reinforcement Learning Is Not All You Need for Deep Research Agents》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的深度研究智能体（Deep Research Agents）高度依赖**在线强化学习（Online RL）**来提升性能。然而，在线 RL 存在三大痛点：1) **成本极高**（频繁调用搜索 API 产生巨额费用）；2) **训练不稳定**（环境非平稳）；3) **难以复现**。
*   **核心问题**：是否可以通过纯**离线训练（Offline Training）**构建出性能媲美甚至超越在线 RL 系统的深度研究智能体？
*   **整体含义**：论文证明了高质量的合成数据和离线偏好优化（DPO）足以训练出强大的研究智能体，从而显著降低了学术界开发此类系统的门槛。

### 2. 方法论
论文提出了一个全栈式的离线训练方案，包含数据合成、数据集构建和模型训练：
*   **DeepForge 任务合成框架**：
    *   **可扩展实体扩展**：不依赖静态维基百科，而是通过 LLM 生成初始名词，利用搜索引擎获取 URL，再从网页内容中提取长尾、低频实体，构建动态实体池。
    *   **复杂问题生成**：在实体图上进行多跳遍历，利用 DeepSeek-v3.1 生成需要多轮搜索、跨实体推理和歧义消除的问题，并进行“线索剪枝”以增加难度。
*   **训练流程**：
    *   **SFT（有监督微调）**：构建 33k 条高质量推理轨迹。使用专有模型在真实互联网环境中交互，经过多阶段过滤（格式、正确性、逻辑一致性）。
    *   **DPO（直接偏好优化）**：构建 21k 对偏好数据。利用 SFT 模型生成多个候选轨迹，由强模型评分，选取高分和低分轨迹组成偏好对。
*   **智能体架构**：采用 ReAct 风格，支持 `search`（搜索）、`visit_urls`（访问网页）、`search_wiki`（维基搜索）和 `code_execution`（代码执行）四种工具。

### 3. 实验设计
*   **数据集/场景**：在六个极具挑战性的基准测试上进行评估：GAIA（通用 AI 助手）、BrowseComp-zh/en（网页浏览推理）、HLE（人类最后考试）、XBench-DeepSearch 和 WebWalkerQA。
*   **对比方法**：
    *   **大型基础模型**：OpenAI-o3, Claude-4-Sonnet, DeepSeek-V3.1。
    *   **中型研究智能体（30B-72B）**：WebSailor, WebShaper, Tongyi-DeepResearch（多采用在线 RL）。
    *   **小型研究智能体（<10B）**：WebExplorer-8B, DeepDive-9B, ASearcher。

### 4. 资源与算力
*   **算力投入**：使用 **64 台 NVIDIA H20 GPU**（每台 80GB 显存）。
*   **训练细节**：
    *   **SFT**：上下文窗口 128k，训练 3 个 Epoch，全局 Batch Size 为 512。
    *   **DPO**：上下文窗口 48k，训练 1 个 Epoch，全局 Batch Size 为 512。
*   **成本对比**：文中提到 50 步 GRPO（在线 RL）训练需花费约 350 美元 API 费，而 OffSeeker 的训练 API 成本接近于零。

### 5. 实验数量与充分性
*   **实验规模**：进行了主实验对比、消融实验及多项实证研究。
*   **充分性分析**：
    *   **数据规模实验**：测试了从 2k 到 33k 不同数据量对性能的影响，证明了数据质量的优越性。
    *   **上下文窗口实验**：对比了 16k 到 128k 窗口的表现，发现 128k 对复杂任务至关重要。
    *   **模型缩放实验**：涵盖了 4B、8B、14B 三种尺寸。
    *   **客观性**：实验覆盖了中英双语及多种任务类型，对比了当前最先进的开源和闭源模型，结果具有较强的说服力。

### 6. 主要结论与发现
*   **离线胜过在线**：8B 规模的 OffSeeker 在多个榜单上不仅领先同尺寸模型，甚至超越了经过重度在线 RL 训练的 30B 级模型（如 WebSailor-32B）。
*   **泛化能力强**：尽管主要针对搜索任务训练，但在 HLE 等非搜索类推理任务上也表现出色。
*   **长上下文是刚需**：在深度研究任务中，128k 窗口的性能远超 32k 窗口（性能提升近 3 倍）。
*   **DPO 的增益**：在 SFT 基础上，离线 DPO 稳定提升了所有基准测试的得分。

### 7. 优点
*   **高效低成本**：打破了“深度研究必用在线 RL”的迷思，极大地降低了研发成本。
*   **全开源贡献**：开源了 DeepForge 框架、66k 任务数据、33k 轨迹数据及模型权重，对社区贡献巨大。
*   **数据合成创新**：DeepForge 能够生成比现有开源数据集（如 ASearcher）难度更高、步骤更长（平均 11.2 步交互）的任务。

### 8. 不足与局限
*   **领域覆盖**：目前主要集中在通用网页搜索，尚未覆盖科学文献检索或多模态研究任务。
*   **上下文瓶颈**：对于极度复杂的查询，即使 128k 窗口也可能不足，缺乏外部记忆或长文本摘要机制。
*   **环境依赖**：虽然训练是离线的，但推理时仍依赖外部搜索 API 和网页解析服务的稳定性。

（完）
