---
title: "Orchestrating Tokens and Sequences: Dynamic Hybrid Policy Optimization for RLVR"
title_zh: 编排 Token 与序列：面向 RLVR 的动态混合策略优化
authors: "Zijun Min, Bingshuai Liu, Ante Wang, Long Zhang, Anxiang Zeng, Haibo Zhang, Jinsong Su"
date: 2026-01-09
pdf: "https://arxiv.org/pdf/2601.05607v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: 具有可验证奖励的强化学习 (RLVR) 算法
tldr: 针对强化学习可验证奖励（RLVR）中 token 级（GRPO）和序列级（GSPO）优化策略的局限性，本文提出动态混合策略优化（DHPO）。该方法通过加权机制融合两种粒度的重要性采样比率，并引入分支特定裁剪策略以增强训练稳定性。在 Qwen3 系列模型上的实验表明，DHPO 在多个数学推理任务中显著优于现有方法，有效平衡了细粒度信用分配与序列级奖励匹配。
selection_source: fresh_fetch
motivation: 现有的 RLVR 算法在 token 级（高方差）和序列级（缺乏细粒度信用分配）优化之间存在权衡，难以兼顾稳定性和精确性。
method: 提出 DHPO 框架，通过平均或熵引导的混合机制结合两种粒度的重要性比率，并采用分支特定裁剪策略来稳定训练过程。
result: 在 Qwen3 系列模型的七个数学推理基准测试中，DHPO 的性能一致且显著地优于 GRPO 和 GSPO。
conclusion: DHPO 通过动态融合不同粒度的策略优化，为提升大语言模型在复杂推理任务中的强化学习效果提供了稳健且高效的方案。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 为优化推理任务中的大语言模型提供了一个极具前景的框架。然而，现有的 RLVR 算法关注不同的粒度，且各自具有互补的优势和局限性。组相对策略优化 (GRPO) 使用 token 级重要性采样比率更新策略，这保留了细粒度的信用分配，但通常面临高方差和不稳定的问题。相比之下，组序列策略优化 (GSPO) 在响应的所有 token 上应用单一的序列级重要性采样比率，能更好地匹配序列级奖励，但牺牲了 token 级的信用分配。在本文中，我们提出了动态混合策略优化 (DHPO)，在单一的裁剪代理目标函数中桥接了 GRPO 和 GSPO。DHPO 利用加权机制结合了 token 级和序列级的重要性采样比率。我们探索了两种混合机制变体，包括平均混合和熵引导混合。为了进一步稳定训练，我们采用了一种特定于分支的裁剪策略，在混合之前将 token 级和序列级比率限制在各自的置信区域内，防止任一分支中的离群值主导更新。在七个具有挑战性的数学推理基准测试中，针对 Qwen3 系列的稠密模型和 MoE 模型进行的实验表明，DHPO 始终优于 GRPO 和 GSPO。我们将在论文被接收后发布代码。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) offers a promising framework for optimizing large language models in reasoning tasks. However, existing RLVR algorithms focus on different granularities, and each has complementary strengths and limitations. Group Relative Policy Optimization (GRPO) updates the policy with token-level importance ratios, which preserves fine-grained credit assignment but often suffers from high variance and instability. In contrast, Group Sequence Policy Optimization (GSPO) applies single sequence-level importance ratios across all tokens in a response that better matches sequence-level rewards, but sacrifices token-wise credit assignment. In this paper, we propose Dynamic Hybrid Policy Optimization (DHPO) to bridge GRPO and GSPO within a single clipped surrogate objective. DHPO combines token-level and sequence-level importance ratios using weighting mechanisms. We explore two variants of the mixing mechanism, including an averaged mixing and an entropy-guided mixing. To further stabilize training, we employ a branch-specific clipping strategy that constrains token-level and sequence-level ratios within separate trust regions before mixing, preventing outliers in either branch from dominating the update. Across seven challenging mathematical reasoning benchmarks, experiments on both dense and MoE models from the Qwen3 series show that DHPO consistently outperforms GRPO and GSPO. We will release our code upon acceptance of this paper.

---

## 论文详细总结（自动生成）

这是一份关于论文《Orchestrating Tokens and Sequences: Dynamic Hybrid Policy Optimization for RLVR》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于**具有可验证奖励的强化学习 (RLVR)**，这在数学推理和编程等任务中至关重要。
*   **核心矛盾**：现有的策略优化方法在“粒度”上存在权衡。
    *   **GRPO (Token级)**：提供细粒度的信用分配，但由于 token 级重要性采样比率波动大，导致训练方差高、不稳定，且容易出现熵崩溃。
    *   **GSPO (序列级)**：通过序列平均比率实现稳定更新，与序列级奖励匹配更好，但忽略了 token 间的差异，缺乏细粒度的信用分配。
*   **研究动机**：能否设计一种方法，既保留 token 级的灵活性，又具备序列级的稳定性？

### 2. 方法论：动态混合策略优化 (DHPO)
DHPO 的核心思想是在单一的裁剪代理目标函数中，动态地融合 token 级和序列级的重要性比率。
*   **混合重要性比率**：定义混合比率 $m_{i,t}(\theta) = w_{i,t} r_{i,t}(\theta) + (1 - w_{i,t}) s_{i,t}(\theta)$。其中 $r$ 是 token 级比率，$s$ 是序列级比率。
*   **两种混合策略**：
    1.  **平均混合 (DHPO-A)**：固定权重 $w=0.5$，简单平衡两种信号。
    2.  **熵引导混合 (DHPO-E)**：利用当前策略的 token 熵作为不确定性信号。当不确定性高时，增加 token 级权重以促进探索；当信心增强时，增加序列级权重以稳定更新。
*   **分支特定裁剪 (Branch-Specific Clipping)**：这是稳定训练的关键。不同于 PPO 统一裁剪，DHPO 对 token 分支和序列分支分别设置独立的信任区域（裁剪阈值），防止某一个分支的离群值（Outliers）主导梯度更新。

### 3. 实验设计
*   **模型**：使用了 Qwen3 系列的三种规模模型：1.7B (Base)、4B (Base) 以及 30B-A3B (MoE 模型)。
*   **数据集/基准测试**：涵盖 7 个具有挑战性的数学推理榜单：AIME 2024/2025、AMC 2023、OlympiadBench、MATH-500、Minerva Math 和 GSM8K。
*   **对比方法**：
    *   **GRPO** (DeepSeek 提出)
    *   **GSPO** (阿里提出)
    *   **GMPO** (几何平均策略优化)
    *   **CISPO** (裁剪重要性采样策略优化)

### 4. 资源与算力
*   **硬件设施**：实验部署在一个包含 **4 个节点**的集群上，每个节点配备 8 张 **NVIDIA H100 GPU**，总计 **32 张 H100**。
*   **软件框架**：使用 VERL 框架进行训练，vLLM 作为推理/采样引擎。
*   **训练细节**：Prompt Batch Size 为 512，每个 Prompt 生成 16 个响应，Actor 学习率为 $1 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 种不同架构/规模的模型上进行了验证，并在 7 个主流基准上测试，实验覆盖面广。
*   **消融实验**：专门对比了“统一裁剪”与“分支特定裁剪”的效果，并分析了训练过程中熵（Entropy）和响应长度的变化曲线。
*   **客观性**：所有对比算法均在相同的 SimpleRL 框架和超参数设置下运行，确保了比较的公平性。实验结果显示 DHPO 在不同规模下均有一致的提升，证明了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：DHPO 在所有模型规模和基准测试中均优于 GRPO 和 GSPO。例如，在 Qwen3-30B-A3B 上，DHPO 将 AIME24 的准确率从 22.5% (GRPO) 提升至 34.4%。
*   **训练稳定性**：DHPO 有效缓解了 GRPO 常见的熵崩溃问题，保持了更健康的训练动态（更高的熵和更长的响应长度）。
*   **混合优势**：熵引导混合 (DHPO-E) 在处理复杂推理任务时表现出更好的自适应能力。

### 7. 优点
*   **理论统一**：成功将 token 级和序列级优化统一到一个数学框架下，并给出了详细的梯度分析。
*   **即插即用**：作为一种目标函数的改进，它可以很容易地集成到现有的 RL 框架中。
*   **解决痛点**：通过分支特定裁剪解决了不同粒度信号数值范围差异大的问题，显著提升了训练稳定性。

### 8. 不足与局限
*   **模型多样性有限**：实验仅限于 Qwen3 系列模型，尚未在 Llama 或其他架构上验证其泛化性。
*   **计算开销**：虽然论文提到熵计算是轻量级的，但动态计算每个 token 的熵并进行混合，相比原生 GRPO 仍会增加一定的计算负担。
*   **超参数敏感性**：引入了更多的裁剪阈值（token 级和序列级需分别设置），可能增加了调参的复杂度。

（完）
