---
title: Continual Policy Distillation from Distributed Reinforcement Learning Teachers
title_zh: 基于分布式强化学习教师的持续策略蒸馏
authors: "Yuxuan Li, Qijun He, Mingqi Yuan, Wen-Tse Chen, Jeff Schneider, Jiayu Chen"
date: 2026-01-30
pdf: "https://arxiv.org/pdf/2601.22475v1"
tags: ["query:rl-ts"]
score: 10.0
evidence: 持续强化学习的师生框架
tldr: 针对持续强化学习（CRL）中灾难性遗忘和扩展性难题，本文提出一种创新的教师-学生框架。该框架将CRL过程解构为两个独立阶段：首先通过分布式强化学习训练多个单任务教师模型，随后将这些策略持续蒸馏至一个中心通用模型中。通过引入混合专家（MoE）架构和重放机制，该方法有效平衡了模型的塑性与稳定性，在Meta-World基准测试中展现了卓越的性能和极低的遗忘率。
selection_source: fresh_fetch
motivation: 旨在解决传统持续强化学习在处理序列任务流时难以平衡稳定性与塑性，以及直接应用RL算法效率低下的问题。
method: 提出一种解耦框架，利用分布式RL训练单任务教师，并结合MoE架构与重放机制将教师策略持续蒸馏至通用模型。
result: "在Meta-World基准上，该框架成功恢复了教师模型85%以上的性能，并将各任务的遗忘率严格控制在10%以内。"
conclusion: 实验证明，将单任务强化学习与持续策略蒸馏相解耦是实现高效、可扩展且抗遗忘的持续强化学习的有效策略。
---

## 摘要
持续强化学习 (CRL) 旨在开发终身学习智能体，使其能够在获取跨多样化任务知识的同时缓解灾难性遗忘。这需要有效地管理稳定性-塑性困境，并利用先验经验快速泛化到新任务。尽管针对这两个方面已经提出了各种增强策略，但通过将强化学习直接应用于顺序任务流来实现可扩展的性能仍然具有挑战性。在本文中，我们提出了一种新颖的教师-学生框架，将 CRL 解耦为两个独立的过程：通过分布式强化学习训练单任务教师模型，并将其持续蒸馏到一个中央通用模型中。这一设计的动机源于以下观察：强化学习擅长解决单任务，而策略蒸馏（一种相对稳定的监督学习过程）与大型基础模型和多任务学习高度契合。此外，采用了混合专家 (MoE) 架构和基于重放的方法来增强持续策略蒸馏过程的塑性和稳定性。在 Meta-World 基准测试上的广泛实验表明，我们的框架能够实现高效的持续强化学习，在恢复超过 85% 的教师性能的同时，将任务遗忘率控制在 10% 以内。

## Abstract
Continual Reinforcement Learning (CRL) aims to develop lifelong learning agents to continuously acquire knowledge across diverse tasks while mitigating catastrophic forgetting. This requires efficiently managing the stability-plasticity dilemma and leveraging prior experience to rapidly generalize to novel tasks. While various enhancement strategies for both aspects have been proposed, achieving scalable performance by directly applying RL to sequential task streams remains challenging. In this paper, we propose a novel teacher-student framework that decouples CRL into two independent processes: training single-task teacher models through distributed RL and continually distilling them into a central generalist model. This design is motivated by the observation that RL excels at solving single tasks, while policy distillation -- a relatively stable supervised learning process -- is well aligned with large foundation models and multi-task learning. Moreover, a mixture-of-experts (MoE) architecture and a replay-based approach are employed to enhance the plasticity and stability of the continual policy distillation process. Extensive experiments on the Meta-World benchmark demonstrate that our framework enables efficient continual RL, recovering over 85% of teacher performance while constraining task-wise forgetting to within 10%.

---

## 论文详细总结（自动生成）

# 关于《Continual Policy Distillation from Distributed Reinforcement Learning Teachers》的论文总结

### 1. 核心问题与整体含义
本文针对**持续强化学习 (Continual Reinforcement Learning, CRL)** 中的核心矛盾——**稳定性-塑性困境 (Stability-Plasticity Dilemma)** 展开研究。在处理序列任务流时，智能体往往难以在学习新技能（塑性）的同时保留旧技能（稳定性/抗遗忘）。
论文的核心动机在于：直接在任务流上应用强化学习算法（如 Actor-Critic）极其不稳定且难以扩展。因此，作者提出将 CRL 过程**解构**为两个独立阶段：先利用分布式 RL 训练单任务专家（教师），再通过监督学习的方式将这些专家策略持续蒸馏到一个中心通用模型（学生）中。这种做法利用了监督学习在训练大型基础模型时的稳定性。

### 2. 方法论
论文提出了一种基于教师-学生架构的持续策略蒸馏框架，关键技术包括：
*   **解耦训练流**：将复杂的持续 RL 任务转化为“分布式单任务 RL 训练”+“持续监督蒸馏”。
*   **Transformer-MoE 架构**：学生模型采用 Transformer 骨干网络，并将 FFN 层替换为**混合专家 (Mixture-of-Experts, MoE)** 模块。
*   **增量专家扩展 (Incremental MoE)**：随着新任务到达，动态增加新的专家网络，并使用“冷启动偏置初始化”确保新专家平滑接入。
*   **对比任务嵌入 (Contrastive Task Embedding)**：利用 InfoNCE 损失函数，从状态-动作序列中学习任务表征，使模型在没有显式任务 ID 的情况下能推断当前任务上下文。
*   **多样性感知轨迹重放**：引入**行列式点过程 (Determinantal Point Processes, DPP)** 算法，从教师演示中筛选出最具代表性和多样性的轨迹（仅占总数据量 <10%）存入重放池，以缓解遗忘。
*   **参数级掩码策略**：
    1.  **骨干冻结**：第一阶段后冻结 Transformer 的共享参数（如 Attention、LN）。
    2.  **两阶段专家训练**：先冻结旧专家训练门控网络和新专家，再冻结门控网络微调所有专家。

### 3. 实验设计
*   **数据集/场景**：使用 **Meta-World (V2)** 机器人操纵基准测试。
*   **Benchmark 设置**：
    *   **MT10**：10个基础任务。
    *   **MT25**：25个多样化操纵任务。
    *   **持续学习设置**：分为 2 阶段（10+15 任务）和更具挑战性的 5 阶段（每阶段 5 个任务）序列学习。
*   **对比方法**：
    *   **基础基线**：Sequential Finetune（顺序微调）、Trac（参数无关优化器）。
    *   **抗遗忘基线**：EWC（弹性权重巩固）、KL 散度约束、纯经验重放 (Replay Only)。
    *   **性能参考**：Independent（独立训练每个任务，代表塑性上限）、Joint Distillation（联合蒸馏所有任务）。

### 4. 资源与算力
*   **训练规模**：每个教师模型（PPO 算法）训练 **10M (一千万)** 交互步；中心学生模型的蒸馏过程在每个阶段训练 **3M** 步。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长（小时/天）。但从 Transformer-MoE 的规模和 Meta-World 的复杂性推断，该实验需要中大型 GPU 集群支持分布式教师训练。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了 2 阶段和 5 阶段的持续学习实验，并针对 MT10 进行了多任务 RL 基线的详尽对比。
*   **消融实验**：非常充分。作者分别验证了任务嵌入 (TE)、专家扩展、重放机制、门控冻结策略等每一个组件对最终性能的影响。
*   **客观性**：实验采用了 3 到 5 个随机种子，并报告了均值和标准差。对比基线涵盖了正则化、重放、架构扩展等主流持续学习类别，评估指标包括平均准确率 (Acc) 和后向转移 (BWT)，实验设计较为公正、客观。

### 6. 主要结论与发现
*   **性能恢复**：该框架能恢复教师模型 **85% 以上** 的性能，在 MT10 上甚至达到了 88.9% 的成功率，优于许多专门的多任务 RL 算法。
*   **抗遗忘效果显著**：在 5 阶段持续学习中，该方法将任务遗忘率控制在 **10% 以内**，且 BWT 指标在某些情况下为正值（2.3%），表明存在正向知识迁移。
*   **解耦的优越性**：证明了将 RL 的探索压力与策略聚合的稳定性压力分开处理，是实现大规模持续 RL 的有效途径。

### 7. 优点
*   **架构扩展性强**：MoE 架构允许模型容量随任务增长而动态扩展，有效解决了固定容量模型的饱和问题。
*   **重放效率高**：通过 DPP 算法筛选轨迹，仅需极小的存储开销即可实现极强的稳定性。
*   **无需任务 ID**：通过对比学习获取任务嵌入，使其更符合现实中任务边界模糊的持续学习场景。

### 8. 不足与局限
*   **教师依赖性**：框架的最终表现高度依赖于单任务教师模型的质量。如果 PPO 教师在某些任务上失败（如文中提到的 disassemble-v3 任务），学生模型也无法通过蒸馏习得该技能。
*   **基准覆盖面**：实验仅限于 Meta-World 机器人操纵任务，尚未在 Atari 游戏或具有高维视觉输入的复杂 3D 环境中验证。
*   **计算成本预置**：虽然蒸馏过程稳定，但前期为每个任务训练独立教师的分布式算力需求较高，不适合算力受限的实时在线学习场景。

（完）
