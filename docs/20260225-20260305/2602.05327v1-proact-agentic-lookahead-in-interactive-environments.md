---
title: "ProAct: Agentic Lookahead in Interactive Environments"
title_zh: ProAct：交互式环境中的智能体前瞻
authors: "Yangbin Yu, Mingyu Yang, Junyou Li, Yiming Gao, Feiyu Liu, Yijun Yang, Zichuan Lin, Jiafei Lyu, Yicheng Liu, Zhicong Lu, Deheng Ye, Jie Jiang"
date: 2026-02-05
pdf: "https://arxiv.org/pdf/2602.05327v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 用于智能体训练的落地前瞻蒸馏 (GLAD)
tldr: 针对大语言模型智能体在长程规划中因模拟误差导致表现不佳的问题，本文提出ProAct框架。该框架通过两阶段训练提升智能体的前瞻推理能力：首先利用GLAD将环境搜索得到的复杂推理链蒸馏至模型中；其次引入MC-Critic，通过轻量级环境回放校准价值估计，优化策略梯度算法。实验表明，ProAct显著提升了规划准确性，4B参数模型在多个环境中表现优异，甚至可媲美闭源模型。
selection_source: fresh_fetch
motivation: 现有LLM智能体在长程规划任务中容易因模拟未来状态时的误差累积而导致决策失败。
method: 提出包含GLAD蒸馏技术和MC-Critic价值评估器的两阶段训练范式，将环境搜索逻辑内化并优化策略梯度。
result: 实验证明ProAct在随机和确定性环境中均显著提升了规划精度，4B模型性能超越了所有开源基准并比肩顶级闭源模型。
conclusion: ProAct通过将搜索能力内化到模型推理中，为提升轻量级智能体在复杂交互环境下的长程规划能力提供了有效路径。
---

## 摘要
现有的语言模型（LLM）智能体在需要长程规划的交互式环境中表现不佳，这主要是由于模拟未来状态时产生的复合误差。为了解决这一问题，我们提出了 ProAct，这是一个通过两阶段训练范式使智能体能够内化准确前瞻推理的框架。首先，我们引入了接地前瞻蒸馏（Grounded LookAhead Distillation, GLAD），使智能体在基于环境搜索生成的轨迹上进行监督微调。通过将复杂的搜索树压缩为简洁的因果推理链，智能体能够在没有推理时搜索计算开销的情况下学习前瞻逻辑。其次，为了进一步提升决策准确性，我们提出了蒙特卡洛评论家（Monte-Carlo Critic, MC-Critic），这是一种即插即用的辅助价值估计器，旨在增强 PPO 和 GRPO 等策略梯度算法。通过利用轻量级的环境展开来校准价值估计，MC-Critic 提供了一个低方差信号，在不依赖昂贵的基于模型的价值近似的情况下，促进了稳定的策略优化。在随机（如 2048）和确定性（如 Sokoban）环境中的实验表明，ProAct 显著提高了规划准确性。值得注意的是，使用 ProAct 训练的 4B 参数模型优于所有开源基准，并可与最先进的闭源模型相媲美，同时在未知环境中展现出强大的泛化能力。代码和模型可在 https://github.com/GreatX3/ProAct 获取。

## Abstract
Existing Large Language Model (LLM) agents struggle in interactive environments requiring long-horizon planning, primarily due to compounding errors when simulating future states. To address this, we propose ProAct, a framework that enables agents to internalize accurate lookahead reasoning through a two-stage training paradigm. First, we introduce Grounded LookAhead Distillation (GLAD), where the agent undergoes supervised fine-tuning on trajectories derived from environment-based search. By compressing complex search trees into concise, causal reasoning chains, the agent learns the logic of foresight without the computational overhead of inference-time search. Second, to further refine decision accuracy, we propose the Monte-Carlo Critic (MC-Critic), a plug-and-play auxiliary value estimator designed to enhance policy-gradient algorithms like PPO and GRPO. By leveraging lightweight environment rollouts to calibrate value estimates, MC-Critic provides a low-variance signal that facilitates stable policy optimization without relying on expensive model-based value approximation. Experiments on both stochastic (e.g., 2048) and deterministic (e.g., Sokoban) environments demonstrate that ProAct significantly improves planning accuracy. Notably, a 4B parameter model trained with ProAct outperforms all open-source baselines and rivals state-of-the-art closed-source models, while demonstrating robust generalization to unseen environments. The codes and models are available at https://github.com/GreatX3/ProAct

---

## 论文详细总结（自动生成）

这是一份关于论文《ProAct: Agentic Lookahead in Interactive Environments》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 智能体在长程规划任务中表现不佳，核心原因在于**“模拟漂移”（Simulation Drift）**。当智能体尝试在脑海中模拟未来状态时，微小的预测误差会随步数增加而迅速累积，导致生成的推理链脱离现实，产生“幻觉规划”。
*   **核心目标**：提出 **ProAct** 框架，旨在通过两阶段训练范式，将昂贵的“系统 2”（慢思考/搜索）能力内化到轻量级模型的“系统 1”（快思考/直觉）中，使智能体在不进行推理时搜索的情况下，依然具备准确的前瞻（Lookahead）推理能力。

### 2. 方法论
ProAct 包含两个关键阶段：

*   **阶段一：接地前瞻蒸馏（GLAD）**
    *   **环境探测**：利用蒙特卡洛树搜索（MCTS）作为“环境先知”，在真实环境中探索多种未来路径（包括最优路径和死胡同）。
    *   **认知压缩**：将复杂的搜索树转化为简洁的自然语言推理链。压缩遵循四个原则：格式简化（去除结构化标签）、显式推理链（观察→分析→结论）、未来趋势估计（解释为何拒绝其他动作）、保留多样性（体现权衡过程）。
    *   **监督微调（SFT）**：在压缩后的高质量推理数据上训练模型，使其学习预测动作后果的逻辑。

*   **阶段二：带蒙特卡洛评论家的在线强化学习（MC-Critic）**
    *   **核心思想**：针对 LLM 训练中 Critic 网络（价值函数）估计方差大、收敛慢的问题，引入一种**无参数的插件式价值估计器**。
    *   **技术细节**：在每个状态下，利用轻量级的随机策略进行 $M$ 次快速环境回放（Rollout），计算平均折扣回报作为该状态的价值估计 $V_{MC}$。
    *   **算法集成**：将 MC-Critic 集成到 PPO 和 GRPO 中（命名为 MC-PPO 和 MC-GRPO）。通过对比组内不同动作的 MC 价值，为策略梯度提供低方差、接地的奖励信号。

### 3. 实验设计
*   **实验场景**：
    *   **2048**：具有随机性的长程规划游戏（单次轨迹可达数百步）。
    *   **Sokoban（推箱子）**：具有确定性逻辑但奖励极其稀疏的经典规划任务。
*   **Benchmark 与对比对象**：
    *   **闭源模型**：GPT-5 (预研版本)、Claude-4.5-Sonnet、Doubao-Seed-1.8 等。
    *   **开源基准**：Qwen3-4B/30B/235B-Instruct。
    *   **RL 变体**：Traj-GRPO、Step-GRPO、Step-PPO 等。
*   **泛化性测试**：在未见过的地图尺寸（3x3）、修改后的目标值（3072）、改变动作空间和符号表示的变体环境中进行测试。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 **AReaL**（一种大规模异步强化学习系统）来支持长程回放。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。但提到 4B 参数模型在 2048 环境中，随机策略回放 1000 条轨迹耗时不到 3 秒，显示了 MC-Critic 的高效性。

### 5. 实验数量与充分性
*   **实验规模**：SFT 阶段收集了 25K（2048）和 8K（Sokoban）样本；RL 阶段进行了数千步的迭代。
*   **充分性**：
    *   涵盖了从 SFT 到 RL 的全流程对比。
    *   针对 MC-Critic 的关键超参数（回放轨迹数 $M$、回放深度 $T$）进行了详细的消融实验。
    *   **公平性**：所有对比实验均在相同的 4B 参数底座（Qwen3-4B）上进行，确保了方法论改进的有效性而非单纯依靠模型规模。
    *   **客观性**：通过多种环境变体验证了模型不是在“背题”，而是真正学习到了规划逻辑。

### 6. 主要结论与发现
*   **性能飞跃**：经过 ProAct 训练的 **4B 模型**在 2048 上的得分（4503.8）远超 Qwen3-235B（634.7），并能与顶级闭源模型（如 Doubao-Seed-1.8）竞争。
*   **RL 稳定性**：MC-Critic 显著稳定了长程任务的 RL 训练过程，解决了传统参数化 Critic 在样本稀疏时难以收敛的问题。
*   **泛化能力**：模型在改变了符号表示和规则微调的变体环境中依然保持高胜率，证明了推理链的“接地性”。

### 7. 优点
*   **高效性**：将 System 2 的搜索能力“固化”在模型权重中，推理时无需昂贵的搜索开销。
*   **即插即用**：MC-Critic 不需要训练额外的神经网络，易于集成到现有的策略梯度框架中。
*   **缓解幻觉**：通过环境反馈强制校准推理过程，有效解决了 LLM 在复杂互动中的“胡言乱语”问题。

### 8. 不足与局限
*   **环境依赖**：该方法高度依赖于一个可快速运行、可重置的**环境模拟器（Oracle）**。在缺乏模拟器的现实任务（如开放域对话或复杂现实决策）中，难以进行大规模 MCTS 采样。
*   **随机策略的局限**：MC-Critic 目前使用随机策略进行回放，在极度复杂的任务中，随机策略可能永远无法触达奖励状态，导致价值估计失效。
*   **算力细节缺失**：未披露具体的训练资源消耗，对于其他研究者评估复现成本不够友好。

（完）
