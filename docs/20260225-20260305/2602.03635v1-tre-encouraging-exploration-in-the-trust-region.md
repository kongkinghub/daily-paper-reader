---
title: "TRE: Encouraging Exploration in the Trust Region"
title_zh: TRE：在置信区域内鼓励探索
authors: "Chao Huang, Yujing Lu, Quangang Li, Shenghe Wang, Yan Wang, Yueyang Zhang, Long Xia, Jiashu Zhao, Zhiyuan Sun, Daiting Shi, Tingwen Liu"
date: 2026-02-03
pdf: "https://arxiv.org/pdf/2602.03635v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 强化学习探索与熵正则化
tldr: 针对大语言模型（LLM）在强化学习中因词表巨大和生成步数长导致的标准熵正则化失效问题，本文提出了信任区域熵（TRE）。该方法通过将探索限制在模型的信任区域内，避免了概率质量向无效长尾词汇的稀释。实验证明，TRE在数学推理、组合搜索和偏好对齐任务上均优于PPO及传统熵正则化方法，显著提升了LLM的探索效率和推理连贯性。
selection_source: fresh_fetch
motivation: 标准熵正则化在LLM中会导致概率质量流向无效的长尾词汇，从而破坏推理的连贯性。
method: 提出信任区域熵（TRE），将探索行为严格限制在模型的信任区域内，以聚焦于合理的候选词。
result: 在数学推理、组合搜索和偏好对齐等多个任务中，TRE的性能一致优于PPO和标准熵正则化基准。
conclusion: TRE有效解决了LLM强化学习中的累积尾部风险问题，是提升大模型探索能力的稳健方案。
---

## 摘要
熵正则化是强化学习（RL）中增强探索的一种标准技术，然而在大语言模型（LLMs）中，它的效果微乎其微，甚至会降低性能。我们将这一失败归因于具有海量词表和长生成周期的 LLM 中固有的累积尾部风险。在这些环境中，标准的全局熵最大化会不加区分地将概率质量稀释到大量无效标记的尾部，而不是集中在合理的候选标记上，从而破坏了连贯的推理。为了解决这个问题，我们提出了置信区域熵（Trust Region Entropy, TRE），这是一种严格在模型置信区域内鼓励探索的方法。在数学推理（MATH）、组合搜索（Countdown）和偏好对齐（HH）任务上的广泛实验表明，TRE 始终优于原始 PPO、标准熵正则化以及其他探索基准。我们的代码可在 https://github.com/WhyChaos/TRE-Encouraging-Exploration-in-the-Trust-Region 获取。

## Abstract
Entropy regularization is a standard technique in reinforcement learning (RL) to enhance exploration, yet it yields negligible effects or even degrades performance in Large Language Models (LLMs). We attribute this failure to the cumulative tail risk inherent to LLMs with massive vocabularies and long generation horizons. In such environments, standard global entropy maximization indiscriminately dilutes probability mass into the vast tail of invalid tokens rather than focusing on plausible candidates, thereby disrupting coherent reasoning. To address this, we propose Trust Region Entropy (TRE), a method that encourages exploration strictly within the model's trust region. Extensive experiments across mathematical reasoning (MATH), combinatorial search (Countdown), and preference alignment (HH) tasks demonstrate that TRE consistently outperforms vanilla PPO, standard entropy regularization, and other exploration baselines. Our code is available at https://github.com/WhyChaos/TRE-Encouraging-Exploration-in-the-Trust-Region.

---

## 论文详细总结（自动生成）

这篇论文由百度与中国科学院等机构合作完成，针对大语言模型（LLM）在强化学习（RL）阶段探索效率低下的问题，提出了“置信区域熵”（Trust Region Entropy, TRE）方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：为什么在传统强化学习中行之有效的“熵正则化”（Entropy Regularization）在 LLM 训练中往往失效甚至导致性能下降？
*   **研究动机**：
    *   **维度灾难与稀疏流形**：LLM 的词表极大（如 15 万个 token），而有效的推理路径在词表空间中是极其稀疏的。
    *   **累积尾部风险**：标准熵正则化会不加区分地将概率质量推向长尾部分的无效 token。在长文本生成中，每一步引入的微小噪声会随步数增加呈指数级累积，最终破坏思维链（CoT）的连贯性。
    *   **实验观察**：论文发现，在短文本任务中熵正则化略有帮助，但在长程推理任务中，它会显著阻碍模型利用长上下文的能力。

### 2. 方法论：置信区域熵 (TRE)
*   **核心思想**：将探索行为严格限制在模型认为“合理”的候选 token 集合（即置信区域）内，而不是在整个词表空间进行全局均匀化。
*   **关键技术细节**：
    1.  **定义置信区域 ($A_{TR}$)**：
        *   **TRE-K**：选取 Logits 最高的固定前 $K$ 个 token。
        *   **TRE-P**：基于核采样（Nucleus Sampling）思想，选取累积概率达到阈值 $P$ 的最小 token 集合（动态大小）。
    2.  **局部熵计算**：仅对置信区域内的 token 进行 Softmax 归一化，计算其局部概率分布 $\pi_{local}$。
    3.  **缩放因子**：为了使局部熵的量级与全局熵相当，引入缩放因子 $\gamma = \log|A| / \log|A_{TR}|$（其中 $|A|$ 是总词表大小）。
    4.  **损失函数**：将缩放后的局部负熵作为正则项加入 PPO 的代理损失函数中：$L_{total} = L_{surr} + \alpha L_{TRE}$。
*   **算法流程**：在每一步生成时，识别置信区域 -> 计算区域内 token 的局部熵 -> 缩放并反向传播，从而在不增加尾部风险的前提下增加 Top 候选词的多样性。

### 3. 实验设计
*   **数据集/场景**：
    *   **数学推理 (MATH)**：考察复杂逻辑推理能力。
    *   **组合搜索 (Countdown)**：考察目标导向的计算与搜索能力。
    *   **偏好对齐 (HH)**：考察模型与人类价值观的一致性（Helpful & Harmless）。
*   **基准模型 (Backbones)**：Qwen2.5-1.5B-Instruct 和 Qwen2.5-7B-Instruct。
*   **对比方法 (Baselines)**：
    *   **Vanilla PPO**：无熵正则化。
    *   **Ent**：标准全局熵正则化。
    *   **Forking-Tokens**：仅在熵值高的步骤进行优化的选择性策略。
    *   **KL-Cov**：基于优势估计与对数概率协方差的选择性 KL 惩罚方法。

### 4. 资源与算力
*   **框架**：使用 VeRL 强化学习框架。
*   **超参数**：全局 Batch Size 为 512，PPO Mini-batch 为 64，每个 Prompt 生成 8 条路线。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量，但提到训练步数在 300-400 步之间，且使用了 7B 规模的模型，推测使用了工业级 GPU 集群（如 A100 或 H100 节点）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 两个量级，并在三种完全不同性质的任务（推理、搜索、对话）上验证。
*   **消融与分析**：
    *   **生成长度分析**：验证了随生成长度增加，标准熵正则化性能崩溃的趋势。
    *   **策略置信度分析**：通过“平均峰值概率”指标展示了 TRE 如何有效缓解过早收敛（模型变得过于自大）。
    *   **超参数敏感性**：对比了不同 $K$ 值和 $P$ 值的影响。
*   **评价**：实验设计较为充分，对比了最新的强基准（如 KL-Cov），结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **TRE 显著优于基准**：在所有任务中，TRE（尤其是 TRE-P 变体）均取得了最佳性能。
*   **动态区域更佳**：TRE-P（基于概率阈值）比 TRE-K（固定数量）更稳健，因为它能根据模型当前的自信程度动态调整探索范围。
*   **缓解过早收敛**：TRE 能有效防止模型在训练早期迅速将概率锁定在单一 token 上，保持了健康的探索空间。
*   **长程推理的必要性**：对于需要长思维链的任务，限制探索区域是维持逻辑连贯性的关键。

### 7. 优点
*   **理论洞察深刻**：准确指出了 LLM 词表稀疏性与长程生成导致的“累积尾部风险”是传统熵正则化失效的根源。
*   **方法简洁高效**：TRE 不需要额外的模型组件，仅通过修改 Logits 的处理方式即可实现，易于集成到现有 PPO 框架中。
*   **通用性强**：在数学、搜索和对齐任务上表现一致，证明了其作为通用 RL 插件的潜力。

### 8. 不足与局限
*   **超长文本验证缺失**：实验最大长度为 8192 token，对于目前流行的超长上下文（如 128k+）推理任务，其稳定性尚待验证。
*   **模型规模限制**：实验最高只做到 7B 参数，在 70B 或更大规模模型上的表现（以及超参数是否需要重新调优）仍需进一步研究。
*   **奖励作弊（Reward Hacking）**：论文明确提到未深入分析奖励作弊问题，这在强化学习中是一个潜在的风险点。

（完）
