---
title: "Breaking the Curse of Repulsion: Optimistic Distributionally Robust Policy Optimization for Off-Policy Generative Recommendation"
title_zh: 破解排斥之咒：面向离策生成式推荐的乐观分布鲁棒策略优化
authors: "Jie Jiang, Yusen Huo, Xiangxin Zhan, Changping Wang, Jun Zhang"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10430v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: 离线历史日志的离策训练与噪声模仿
tldr: 针对生成式推荐中离线强化学习面临的低质量数据导致模型崩溃问题，本文提出了排斥优化发散理论，揭示了负梯度更新在离线训练中引发的强度爆炸现象。为此，研究者将目标重构为乐观分布鲁棒优化（DRO）问题，并提出DRPO算法。该方法通过硬过滤机制从噪声行为策略中提取高质量分布，有效解决了噪声模仿与方差缩减的矛盾，在混合质量推荐基准上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 离线历史日志中存在大量低质量数据，导致传统的策略梯度方法在训练时容易出现模型崩溃和发散。
method: 提出分布鲁棒策略优化（DRPO），利用乐观分布鲁棒优化框架和硬过滤机制从噪声数据中提取高质量分布。
result: 实验结果表明，DRPO 在多个混合质量的推荐系统基准测试中均显著优于现有的离线强化学习方法。
conclusion: 该研究通过揭示排斥优化的发散本质，证明了硬过滤是解决离线生成式推荐中噪声干扰问题的最优方案。
---

## 摘要
基于策略的强化学习（RL）已成为生成式推荐中优化序列用户交互的主流范式。然而，当应用于离线历史日志时，这些方法面临一个关键失效：低质量数据的主导会导致严重的模型崩溃。我们首先建立了排斥优化的发散理论，揭示了在离策训练过程中，负梯度更新本质上会引发指数级的强度爆炸。该理论阐明了现有方法的内在困境，揭示了它们无法调和方差缩减与噪声模仿之间的矛盾。为了破解这一困境，我们认为解决方案在于严格识别纠缠在噪声行为策略中的潜在高质量分布。据此，我们将目标重新表述为一个乐观分布鲁棒优化（DRO）问题。在此公式的指导下，我们提出了分布鲁棒策略优化（DRPO）。我们证明了硬过滤是该 DRO 目标的精确解，使 DRPO 能够最优地恢复高质量行为，同时严格剔除导致发散的噪声。广泛的实验表明，DRPO 在混合质量的推荐基准测试中达到了最先进的性能。

## Abstract
Policy-based Reinforcement Learning (RL) has established itself as the dominant paradigm in generative recommendation for optimizing sequential user interactions. However, when applied to offline historical logs, these methods suffer a critical failure: the dominance of low-quality data induces severe model collapse. We first establish the Divergence Theory of Repulsive Optimization, revealing that negative gradient updates inherently trigger exponential intensity explosion during off-policy training. This theory elucidates the inherent dilemma of existing methods, exposing their inability to reconcile variance reduction and noise imitation. To break this curse, we argue that the solution lies in rigorously identifying the latent high-quality distribution entangled within the noisy behavior policy. Accordingly, we reformulate the objective as an Optimistic Distributionally Robust Optimization (DRO) problem. Guided by this formulation, we propose Distributionally Robust Policy Optimization (DRPO). We prove that hard filtering is the exact solution to this DRO objective, enabling DRPO to optimally recover high-quality behaviors while strictly discarding divergence-inducing noise. Extensive experiments demonstrate that DRPO achieves state-of-the-art performance on mixed-quality recommendation benchmarks.

---

## 论文详细总结（自动生成）

### 论文总结：破解排斥之咒（Breaking the Curse of Repulsion）

#### 1. 核心问题与整体含义
*   **研究动机**：生成式推荐将推荐建模为连续向量空间中的决策过程。虽然离线强化学习（Offline RL）可以利用历史日志避免从零开始探索，但面临严重的“数据诅咒”：高奖励信号极度稀疏，而低质量噪声占据主导（呈齐普夫分布/Zipfian）。
*   **核心问题**：作者发现，标准的基于策略的 RL 方法在处理这些低质量数据时，会产生负的优势值（Advantage），从而对模型产生“排斥力”。这种排斥力在离策（Off-policy）训练中会导致梯度爆炸和模型崩溃，使得模型无法在噪声环境中收敛。

#### 2. 方法论
*   **核心思想**：将学习目标从“拟合噪声分布”转变为“恢复潜在的高质量分布”。通过将目标重构为**乐观分布鲁棒优化（Optimistic DRO）**问题，证明了“硬过滤”（Hard Filtering）是该问题的数学精确解。
*   **关键技术细节**：
    *   **排斥发散理论（Divergence Theory）**：从数学上证明了负梯度更新在连续空间中会驱动策略参数（均值和方差）发生指数级强度爆炸。
    *   **乐观 DRO 建模**：寻找一个与行为策略接近但在奖励上表现最优的子分布 $Q$。
    *   **硬过滤机制**：通过变分对偶形式求解 CVaR，自动确定奖励阈值 $\nu^*$，仅保留 Top-$\kappa$ 的高质量样本进行训练，彻底切断负梯度带来的排斥力。
    *   **方差引导的动态课程学习**：根据批次奖励的方差动态调整过滤比例 $\kappa$，在训练初期保持稳定，在后期追求卓越。
*   **算法流程**：采样批次数据 -> 计算奖励分布的变分阈值 -> 剔除阈值以下的样本 -> 对剩余高质量样本进行加权回归优化。

#### 3. 实验设计
*   **实验场景**：使用高保真模拟器 **RecSim**，模拟工业级推荐系统的拓扑结构和噪声特性（128维连续嵌入空间）。
*   **测试场景**：
    1.  **中等质量（Medium Quality）**：模拟曝光点击日志，包含偏置但有信号。
    2.  **极端噪声（Extreme Noisy）**：模拟全库检索阶段，噪声占比高达 90%。
*   **Benchmark 与对比方法**：对比了 8 种代表性算法，包括：
    *   标准离线 RL：APG。
    *   正则化模仿学习：BC, CRR, BPPO。
    *   软加权方法：AWR, IQL, AsymRe。
    *   消融基准：Adaptive BC。

#### 4. 资源与算力
*   **算力说明**：论文提到所有算法均使用 **PyTorch** 在**单块 NVIDIA GPU** 上实现。
*   **具体细节**：文中未明确给出具体的 GPU 型号（如 A100 或 V100）、具体的训练总时长或能耗数据。但提到该方法计算开销极小，仅增加了一个批次排序（$O(N \log N)$）的复杂度。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验覆盖了两种典型工业场景。
    *   **消融实验**：逐步验证了硬过滤、软基准和自适应机制的贡献。
    *   **鲁棒性测试**：进行了 0% 到 80% 不同噪声比例下的压力测试。
    *   **动态演化实验**：展示了从离线初始化到在线微调的完整生命周期表现。
*   **充分性评价**：实验设计非常充分且具有说服力。作者不仅对比了最终性能，还通过“幻影梯度”监控等手段验证了其理论推导的正确性，实验逻辑严密，对比基准涵盖了当前主流的离线 RL 范式。

#### 6. 主要结论与发现
*   **理论发现**：负梯度更新在离策训练中是导致模型不稳定的根本原因，软加权（Soft-weighting）只能缓解但不能消除这种发散风险。
*   **核心结论**：**硬过滤（Hard Filtering）**是处理重尾噪声数据的数学最优解。
*   **性能表现**：DRPO 在极端噪声环境下表现极其稳健，而传统方法（如 AWR, IQL）在噪声超过一定比例后会发生性能塌陷。
*   **在线衔接**：DRPO 能够实现平滑的离线到在线过渡，避免了强化学习微调初期常见的性能跌落。

#### 7. 优点
*   **理论深刻**：不仅提出了算法，还建立了“排斥优化发散理论”，为离线 RL 的失效提供了严谨的数学解释。
*   **简单高效**：DRPO 逻辑清晰，不依赖复杂的集成模型（Ensemble）或高成本的价值估计，易于在工业界落地。
*   **鲁棒性极强**：在 90% 噪声的极端环境下依然能提取出有效的专家信号。

#### 8. 不足与局限
*   **实验环境限制**：虽然使用了高保真模拟器，但缺乏在真实大规模生产环境中的 A/B 测试数据。
*   **动作空间假设**：主要针对连续向量空间（生成式推荐），对于离散动作空间或其他类型的 RL 任务（如复杂多步决策）的泛化性仍需进一步验证。
*   **超参数依赖**：虽然有自适应机制，但目标方差比例 $\rho$ 等超参数可能仍需针对不同业务场景进行微调。

（完）
