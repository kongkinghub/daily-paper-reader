---
title: "RAPO: Expanding Exploration for LLM Agents via Retrieval-Augmented Policy Optimization"
title_zh: RAPO：通过检索增强策略优化扩展大语言模型智能体的探索
authors: "Siwei Zhang, Yun Xiong, Xi Chen, Zi'an Jia, Renhong Huang, Jiarong Xu, Jiawei Zhang"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03078v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: 强化学习算法与策略优化
tldr: 针对大语言模型智能体在强化学习中探索受限的问题，本文提出检索增强策略优化（RAPO）框架。该框架通过引入外部检索机制，在训练中结合混合策略展开和检索感知优化，使智能体能基于细粒度的外部步骤进行更广泛的探索。实验证明，RAPO在多项任务上显著提升了性能和训练效率，为复杂推理任务提供了更强的探索能力。
selection_source: fresh_fetch
motivation: 现有的智能体强化学习主要依赖同策略探索，限制了新推理视角的发现，且缺乏对细粒度步骤级探索动态的有效利用。
method: 提出包含混合策略智能体展开和检索感知策略优化的两阶段框架，通过检索外部步骤级轨迹来扩展推理感受野并校准策略梯度。
result: "在三个推理任务的14个数据集上，RAPO实现了5.0%的平均性能提升，且训练速度提高了1.2倍。"
conclusion: RAPO通过将检索集成到强化学习循环中，有效解决了智能体探索不足的问题，为提升LLM智能体的推理能力提供了高效的新方案。
---

## 摘要
智能体强化学习（Agentic RL）在基于大语言模型（LLM）的智能体中展现出巨大的潜力。这些工作使 LLM 智能体能够通过多步、集成工具的推理来处理复杂任务。然而，现有 Agentic RL 方法的一个固有局限是它们依赖纯同策（on-policy）范式进行探索，这限制了探索仅限于智能体自身生成的输出，阻碍了发现新的推理视角以实现进一步提升。虽然最近的研究引入了辅助的异策（off-policy）信号来增强探索，但它们通常利用完整的异策轨迹进行轨迹级策略估计，忽略了智能体展开（rollout）过程中细粒度、步级探索动态的必要性。在本文中，我们重新审视了 Agentic RL 中的探索问题，并提出了检索增强策略优化（RAPO），这是一种新型强化学习框架，通过引入检索在训练期间显式地扩展探索。为实现这一目标，我们将 Agentic RL 训练过程分解为两个阶段：(i) 混合策略智能体展开，以及 (ii) 检索感知策略优化。具体而言，我们提出了一种混合策略智能体展开策略，允许智能体在检索到的异策步级轨迹上持续推理。它动态地扩展了智能体的推理感受野，使其能够基于外部行为进行更广泛的探索。随后，我们引入了检索感知策略优化机制，该机制利用检索奖励和重要性整形（importance shaping）来校准策略梯度估计，从而稳定训练并优先考虑由检索启发的探索。大量实验表明，RAPO 在三个智能体推理任务的 14 个数据集上实现了 +5.0% 的平均增益，同时训练效率提升了 1.2 倍。

## Abstract
Agentic Reinforcement Learning (Agentic RL) has shown remarkable potential in large language model-based (LLM) agents. These works can empower LLM agents to tackle complex tasks via multi-step, tool-integrated reasoning. However, an inherent limitation of existing Agentic RL methods is their reliance on a pure on-policy paradigm for exploration, restricting exploration to the agent's self-generated outputs and preventing the discovery of new reasoning perspectives for further improvement. While recent efforts incorporate auxiliary off-policy signals to enhance exploration, they typically utilize full off-policy trajectories for trajectory-level policy estimation, overlooking the necessity for the fine-grained, step-level exploratory dynamics within agentic rollout. In this paper, we revisit exploration in Agentic RL and propose Retrieval-Augmented Policy Optimization (RAPO), a novel RL framework that introduces retrieval to explicitly expand exploration during training. To achieve this, we decompose the Agentic RL training process into two phases: (i) Hybrid-policy Agentic Rollout, and (ii) Retrieval-aware Policy Optimization. Specifically, we propose a Hybrid-policy Agentic Rollout strategy, which allows the agents to continuously reason over the retrieved off-policy step-level traces. It dynamically extends the reasoning receptive field of agents, enabling broader exploration conditioned on external behaviors. Subsequently, we introduce the Retrieval-aware Policy Optimization mechanism, which calibrates the policy gradient estimation with retrieval reward and importance shaping, stabilizing training and prioritizing retrieval-illuminating exploration. Extensive experiments show that RAPO achieves an +5.0% average gain on fourteen datasets across three agentic reasoning tasks, while delivering 1.2x faster training efficiency.

---

## 论文详细总结（自动生成）

这是一份关于论文《RAPO: Expanding Exploration for LLM Agents via Retrieval-Augmented Policy Optimization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**智能体强化学习（Agentic RL）中的探索受限问题**。
*   **研究背景**：现有的 Agentic RL 方法（如基于 GRPO 的优化）主要依赖“同策略（On-policy）”探索，即智能体只能从自己生成的推理轨迹中学习。
*   **核心痛点**：这种模式导致探索空间被局限在模型自身的初始行为边界内，难以发现更优的推理视角。虽然有些方法引入了“异策略（Off-policy）”信号，但通常是在轨迹级别进行静态估计，缺乏对智能体多步推理中**细粒度、步骤级（Step-level）**动态探索的显式引导。
*   **整体含义**：RAPO 旨在通过**检索增强**的方式，在训练过程中动态地为智能体引入外部高质量的推理步骤，从而显式地扩展其探索视野。

### 2. 方法论：核心思想与关键技术
RAPO 将训练过程分为两个关键阶段：

*   **混合策略智能体展开（Hybrid-policy Agentic Rollout）**：
    *   **步级轨迹缓存（Step-Trace Buffer）**：预先收集异策智能体生成的高质量、步骤级推理轨迹，以“历史上下文-当前步骤”的键值对形式存储。
    *   **检索式推理**：在智能体进行多步推理（Rollout）时，以一定概率（如 0.5）触发检索。系统根据当前推理历史，从缓存中检索最相似的异策步骤并直接插入当前上下文。
    *   **核心思想**：通过强制智能体在外部优秀步骤的基础上继续推理，动态扩展其“推理感受野”，使其接触到自身分布之外的探索路径。

*   **检索感知策略优化（Retrieval-aware Policy Optimization）**：
    *   **检索奖励（Retrieval Reward）**：利用**熵（Entropy）**的变化来量化检索的贡献。如果检索后的步骤显著降低了模型后续推理的不确定性（熵减），且发生在模型原本高度困惑的时刻，则给予高奖励。
    *   **检索重要性整形（Retrieval Importance Shaping）**：针对检索引入的异策 Token 无法传导梯度的问题，通过检索 Token 的占比来重新调整同策 Token 的梯度权重，确保模型能有效吸收检索带来的探索增益。
    *   **训练目标**：结合结果奖励（Outcome Reward）和检索奖励，利用改进的 GRPO 算法进行策略更新。

### 3. 实验设计
*   **数据集与场景**：涵盖 3 大类任务，共 14 个数据集。
    *   **计算推理**：GSM8K, MATH, MATH500, AIME24, AIME25。
    *   **知识密集型推理**：WebWalkerQA, HotpotQA, 2WikiMultihopQA, Musique, Bamboogle。
    *   **Web 智能体推理**：SimpleQA, GAIA, BrowseComp 等。
*   **对比方法（13 种）**：
    *   **工具集成类**：Search-o1, Search-R1, ToolStar。
    *   **异策学习类**：SFT, RolloutReplay, LUFFY。
    *   **强化学习类**：单步 RL（GRPO, DAPO, GPPO）和多步 Agentic RL（GiGPO, Tree-GRPO, ARPO, AEPO）。
*   **底座模型**：Qwen2.5-3B/7B-Instruct, Llama3-8B-Instruct。

### 4. 资源与算力
*   **硬件设备**：
    *   效率研究实验：使用了 **4 张 NVIDIA RTX A100 (80GB) GPU**。
    *   主实验与训练：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：使用了 VeRL 框架。计算与知识推理任务的 Batch Size 为 32，Web 智能体任务为 128。文中未明确给出总训练时长，但强调了效率提升。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，不仅在 14 个数据集上验证了最终性能，还进行了多维度的深入分析。
*   **充分性验证**：
    *   **消融实验**：逐一验证了检索奖励、重要性整形、步级检索（对比轨迹级检索）等组件的有效性。
    *   **鲁棒性测试**：测试了不同质量的异策缓存（Buffer）以及在检索结果包含噪声（随机扰动）时的模型表现。
    *   **效率分析**：对比了 Rollout 时间、Token 生成量和工具调用次数。
*   **客观性**：通过在不同规模（3B, 7B, 8B）和不同架构（Qwen, Llama）的模型上取得一致性提升，证明了方法的普适性和公平性。

### 6. 主要结论与发现
*   **性能提升**：RAPO 在 14 个数据集上平均取得了 **5.0%** 的性能增益，刷新了多项任务的 SOTA。
*   **效率优势**：相比纯同策的 GRPO，RAPO 的训练效率提升了 **1.2 倍**。这主要得益于检索替代了部分 Token 生成，且检索 Token 掩码减少了梯度计算开销。
*   **探索多样性**：可视化分析显示，混合策略展开产生的轨迹在语义空间中形成的聚类更多，证明其显著增加了探索的多样性。
*   **自我调节能力**：即使检索到的是随机噪声，RAPO 仍能通过检索奖励机制识别并保持优于基准的效果，显示出极强的鲁棒性。

### 7. 优点
*   **创新的探索机制**：打破了 Agentic RL 长期依赖同策生成的僵局，通过检索显式地“喂”给模型新的推理思路。
*   **细粒度控制**：将检索集成在步骤级而非轨迹级，保留了智能体推理的动态交互特性。
*   **训练效率双赢**：通常增加探索会降低效率，但 RAPO 通过检索机制反而减少了生成和计算负担，实现了性能与速度的双重提升。
*   **理论支撑**：附录中提供了基于信息瓶颈（Information Bottleneck）理论的分析，增强了方法的科学性。

### 8. 不足与局限
*   **依赖外部缓存**：RAPO 的上限在一定程度上受限于异策缓存（Step-Trace Buffer）的质量。如果缺乏高质量的外部推理轨迹，提升效果可能会打折扣。
*   **存储开销**：需要维护一个大规模的步骤级索引数据库，对于超大规模训练任务，存储和检索的内存开销是一个潜在挑战。
*   **应用限制**：目前主要针对具有明确步骤和工具调用的推理任务，对于开放式、非结构化的对话任务，如何定义“步骤级检索”仍需进一步探索。

（完）
