---
title: "STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning"
title_zh: STAIRS-Former：用于离线多任务多智能体强化学习的交错递归结构时空注意力 Transformer
authors: "Jiwon Jeon, Myungsik Cho, Youngchul Sung"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11691v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 离线多智能体强化学习框架
tldr: 针对离线多任务多智能体强化学习中智能体数量多变及长时序依赖建模难的问题，本文提出STAIRS-Former。该架构通过交错递归结构引入空间和时间层级注意力机制，有效捕捉智能体间的协作关系与长程历史信息，并结合令牌丢弃技术增强泛化性，在多个主流基准测试中达到SOTA性能。
selection_source: fresh_fetch
motivation: 现有离线多智能体强化学习方法在处理多任务时，难以有效利用注意力机制进行智能体间协作，且对长时序依赖的建模能力有限。
method: 提出一种具有空间和时间层级结构的Transformer架构，通过交错递归结构和令牌丢弃技术来优化关键信息的注意力分配与模型鲁棒性。
result: 在SMAC、SMAC-v2、MPE和MaMuJoCo等多个多智能体基准测试中，该方法一致优于现有技术，刷新了性能记录。
conclusion: STAIRS-Former通过创新的时空层级设计，显著提升了离线多任务多智能体强化学习的协作效率与泛化能力。
---

## 摘要
由于不同任务中智能体数量的变化以及对未知场景泛化的需求，利用多任务数据集进行离线多智能体强化学习（MARL）具有挑战性。先前的研究采用具有观测标记化（observation tokenization）和分层技能学习的 Transformer 来解决这些问题。然而，它们未能充分利用 Transformer 的注意力机制进行智能体间的协作，且依赖于单一的历史标记（history token），这限制了它们在部分可观测 MARL 环境中捕捉长时程时间依赖关系的能力。在本文中，我们提出了 STAIRS-Former，这是一种增强了空间和时间层级的 Transformer 架构，能够在捕捉长交互历史的同时，实现对关键标记的有效关注。我们进一步引入了标记丢弃（token dropout），以增强在不同智能体群体规模下的鲁棒性和泛化能力。在包括 SMAC、SMAC-v2、MPE 和 MaMuJoCo 在内的多种多智能体基准测试上进行的大量多任务数据集实验表明，STAIRS-Former 一贯优于先前的方法，并取得了新的最先进性能。

## Abstract
Offline multi-agent reinforcement learning (MARL) with multi-task datasets is challenging due to varying numbers of agents across tasks and the need to generalize to unseen scenarios. Prior works employ transformers with observation tokenization and hierarchical skill learning to address these issues. However, they underutilize the transformer attention mechanism for inter-agent coordination and rely on a single history token, which limits their ability to capture long-horizon temporal dependencies in partially observable MARL settings. In this paper, we propose STAIRS-Former, a transformer architecture augmented with spatial and temporal hierarchies that enables effective attention over critical tokens while capturing long interaction histories. We further introduce token dropout to enhance robustness and generalization across varying agent populations. Extensive experiments on diverse multi-agent benchmarks, including SMAC, SMAC-v2, MPE, and MaMuJoCo, with multi-task datasets demonstrate that STAIRS-Former consistently outperforms prior methods and achieves new state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**离线多任务多智能体强化学习（Offline MT-MARL）**。
*   **研究动机**：在现实场景（如无人机群、自动驾驶）中，智能体数量往往随任务变化，且环境具有部分可观测性。
*   **现有问题**：之前的 Transformer 架构（如 UPDeT）虽然能处理变长输入，但存在两个缺陷：
    1.  **注意力机制利用不足**：注意力分布往往过于均匀，无法有效捕捉智能体间的复杂协作关系。
    2.  **时序建模能力弱**：仅依赖单一的历史标记（History Token），其更新方式类似于简单 RNN，难以捕捉部分可观测环境下的长程依赖。
*   **核心目标**：增强 Transformer 的时空建模能力，使其在多任务场景下具有更强的泛化性和鲁棒性。

### 2. 方法论：STAIRS-Former
STAIRS-Former 引入了空间和时间上的双重层级结构：
*   **空间递归模块（Spatial Recursive Module）**：
    *   采用递归 Transformer 结构，在每一层内进行多次递归更新。
    *   通过参数共享增加模型深度，强化实体间的关系推理，使注意力更聚焦于关键实体。
*   **时间层级模块（Temporal Module）**：
    *   **双尺度历史状态**：维护一个每步更新的低级历史（$h_L$）和一个每 $T_H$ 步通过 GRU 更新的高级历史（$h_H$）。
    *   **时间特征学习（TFL）**：在 Transformer 内部为观测标记和历史标记设置独立的反馈前馈网络（FFN），防止空间关系和时间背景特征相互干扰。
*   **令牌丢弃机制（Token-Dropout）**：
    *   训练期间随机丢弃部分实体令牌（保留自身和历史令牌），模拟不同数量的智能体环境，增强模型对未知实体配置的泛化能力。
*   **训练目标**：结合 **TD3+BC** 损失函数，在优化 Q 值的同时，通过行为克隆（BC）项约束策略不偏离离线数据集分布。

### 3. 实验设计
*   **数据集与场景**：
    *   **SMAC (StarCraft II)**：包括 Marine-Easy, Marine-Hard, Stalker-Zealot 三个任务集，涵盖同构/异构、对称/非对称场景。
    *   **SMAC-v2**：更具随机性和挑战性的版本（Terran, Protoss, Zerg）。
    *   **MPE (Cooperative Navigation)**：基础协作任务。
    *   **MaMuJoCo (HalfCheetah)**：连续控制任务。
*   **数据质量**：涵盖 Expert, Medium, Medium-Expert, Medium-Replay 四种质量。
*   **对比方法（Baselines）**：
    *   **UPDeT-m**：基于 Transformer 的基础离线 MARL。
    *   **ODIS**：学习任务不变的协作技能。
    *   **HiSSD**：当前 SOTA，通过分层框架学习共享和任务特定技能。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090** GPU (24GB 显存)。
*   **训练时长**：在相同设置下，STAIRS-Former 训练约 **4 小时**，而 SOTA 方法 HiSSD 需要约 7 小时 20 分钟。
*   **参数量**：STAIRS-Former 约 22 万参数，远低于 HiSSD 的 68 万参数，显存占用也更低（约 14GB vs 17GB）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大类基准测试、数十个子任务（包括已见和未见任务）上进行了测试。
*   **充分性**：
    *   每个实验均使用 5 个随机种子取平均值。
    *   进行了详尽的**消融实验**，验证了空间递归、时间层级和令牌丢弃各自的贡献。
    *   引入了**休眠神经元分析（Dormant Neuron Analysis）**，从模型容量利用率角度解释了性能提升的原因。
    *   提供了**注意力热图可视化**，直观展示了模型在战斗中的策略（如集火、拉扯）。
*   **客观性**：对比了相同深度的 Transformer，证明提升源于架构设计而非单纯增加层数。

### 6. 主要结论与发现
*   **性能领先**：STAIRS-Former 在几乎所有基准测试中均达到 SOTA，尤其在未见任务（Unseen Tasks）上表现出极强的泛化能力。
*   **时空协同**：空间层级对已见任务提升显著，而时间层级和令牌丢弃对未见任务的泛化至关重要。
*   **效率优势**：相比 HiSSD，该方法在参数量减少 67% 的情况下，性能提升了约 10%~48%（视任务而定）。
*   **策略可解释性**：注意力机制能自发学习到“集火攻击”和“保护残血队友”等高级协作策略。

### 7. 优点
*   **架构创新**：通过交错递归和双尺度时间建模，解决了 Transformer 在 MARL 中注意力涣散的问题。
*   **泛化性强**：Token-Dropout 简单有效，显著提升了跨任务（智能体数量变化）的鲁棒性。
*   **高效率**：在更少的计算资源和参数下实现了更优的性能，具有实际应用潜力。
*   **分析深入**：不仅有结果对比，还通过神经元活性分析等手段深入探讨了模型内部机制。

### 8. 不足与局限
*   **小数据集限制**：在极小规模的数据集（如 Marine-Easy 的 Medium-Replay）上，性能提升不明显，甚至略逊于基准，说明模型对数据多样性仍有一定依赖。
*   **令牌化依赖**：方法依赖于对观测值的实体分解（Entity Decomposition），对于无法轻易分解为实体的复杂观测空间，可能需要额外的预处理。
*   **超参数敏感性**：时间层级中的更新频率 $T_H$ 需要根据任务动态调整，虽然文中给出了推荐值，但在全新领域可能需要调优。

（完）
