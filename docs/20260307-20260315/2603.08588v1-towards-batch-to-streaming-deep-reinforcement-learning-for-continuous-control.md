---
title: Towards Batch-to-Streaming Deep Reinforcement Learning for Continuous Control
title_zh: 迈向连续控制的批处理到流式深度强化学习
authors: "Riccardo De Monte, Matteo Cederle, Gian Antonio Susto"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08588v1"
tags: ["query:rl-ts"]
score: 8.0
evidence: "新型流式强化学习算法 (S2AC, SDAC)"
tldr: 针对深度强化学习在资源受限硬件上计算开销大的问题，本文提出了S2AC和SDAC两种流式深度强化学习算法。该方法通过纯在线更新取代传统的经验回放和批处理，旨在实现从离线批处理到在线流式学习的平滑过渡，特别适用于Sim2Real等设备端微调场景，在保持高性能的同时显著降低了计算资源需求。
selection_source: fresh_fetch
motivation: 传统深度强化学习依赖经验回放和批更新，导致其在资源受限的硬件上难以进行实时在线微调。
method: 提出了S2AC和SDAC算法，通过流式在线更新机制替代批处理，并设计了兼容批处理RL的架构以支持平滑切换。
result: 在标准基准测试中，该算法无需繁琐调参即可达到与现有流式基准相当的性能。
conclusion: 该研究为从批处理到流式学习的过渡提供了有效策略，证明了流式RL在设备端微调和Sim2Real迁移中的实用价值。
---

## 摘要
尖端的深度强化学习（RL）方法在连续控制任务中取得了显著性能，但由于依赖经验回放池（replay buffers）、批更新（batch updates）和目标网络（target networks），其计算复杂度往往与资源受限硬件的约束不相容。新兴的流式深度强化学习范式通过纯在线更新解决了这一局限，在标准基准测试中取得了强劲的经验性能。在这项工作中，我们提出了两种新型流式深度强化学习算法：流式软 Actor-Critic（S2AC）和流式确定性 Actor-Critic（SDAC），它们被明确设计为与尖端的批处理强化学习方法兼容，使其特别适用于设备端微调应用，如 Sim2Real 迁移。两种算法在标准基准测试中均达到了与尖端流式基准相当的性能，且无需繁琐的超参数调优。最后，我们进一步研究了微调过程中从批处理学习过渡到流式学习的实际挑战，并提出了应对这些挑战的具体策略。

## Abstract
State-of-the-art deep reinforcement learning (RL) methods have achieved remarkable performance in continuous control tasks, yet their computational complexity is often incompatible with the constraints of resource-limited hardware, due to their reliance on replay buffers, batch updates, and target networks. The emerging paradigm of streaming deep RL addresses this limitation through purely online updates, achieving strong empirical performance on standard benchmarks. In this work, we propose two novel streaming deep RL algorithms, Streaming Soft Actor-Critic (S2AC) and Streaming Deterministic Actor-Critic (SDAC), explicitly designed to be compatible with state-of-the-art batch RL methods, making them particularly suitable for on-device finetuning applications such as Sim2Real transfer. Both algorithms achieve performance comparable to state-of-the-art streaming baselines on standard benchmarks without requiring tedious hyperparameter tuning. Finally, we further investigate the practical challenges of transitioning from batch to streaming learning during finetuning and propose concrete strategies to tackle them.

---

## 论文详细总结（自动生成）

这篇论文针对连续控制任务，探讨了如何将深度强化学习（DRL）从传统的“批处理”模式转向更适合边缘设备的“流式”模式。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：目前的尖端深度强化学习算法（如 SAC, TD3）高度依赖**经验回放池（Replay Buffer）**、**批更新（Batch Updates）**和**目标网络（Target Networks）**。这些机制虽然保证了训练稳定性，但内存和计算开销巨大，难以在资源受限的嵌入式硬件（如小型机器人、边缘设备）上运行。
*   **研究动机**：
    *   **在线微调需求**：在 Sim2Real（仿真到现实）迁移中，机器人需要在真实环境中进行在线微调以弥补动力学差异。
    *   **兼容性缺失**：现有的流式 RL 算法（如 Stream AC）与主流的批处理算法架构不兼容，导致无法直接利用预训练好的批处理模型进行流式微调。
    *   **计算灵活性**：探索在计算预算波动时，如何在高性能的批处理模式和低功耗的流式模式之间动态切换。

### 2. 方法论
论文提出了两种新型流式算法，旨在将批处理算法的优势引入纯在线更新环境：
*   **S2AC (Streaming Soft Actor-Critic)**：
    *   **核心思想**：将 SAC 改造为流式版本。取消回放池和目标网络，直接在当前样本上更新。
    *   **关键技术**：使用**资格迹（Eligibility Traces）**来增强信用分配；引入 **ObGD（超调限制梯度下降）** 优化器替代 Adam 以增强稳定性；提出**自适应熵系数（$\alpha/\sigma_r$）**，根据奖励缩放比例动态调整探索强度。
*   **SDAC (Streaming Deterministic Actor-Critic)**：
    *   **核心思想**：基于 TD3 的流式确定性策略梯度算法。
    *   **关键技术**：首次在流式框架下应用确定性策略梯度（DPG）；在目标值计算中加入**目标噪声（Target Noise）**以防止 Critic 对价值估计过拟合；同样使用资格迹和 ObGD 优化器。
*   **通用改进**：两算法均采用了层归一化（LayerNorm）、稀疏初始化、状态归一化和奖励缩放，以应对流式学习中的非平稳性。

### 3. 实验设计
*   **实验场景**：
    *   **标准基准测试**：MuJoCo Gym（如 Humanoid, Ant, Hopper）和 DM Control Suite（如 Dog-walk, Cheetah-run）。
    *   **微调场景**：在扰动环境（模拟真实世界磨损或动力学变化）中进行微调，如 `walker-run` 和 `dog-walk`。
*   **对比基准（Benchmarks）**：
    *   **流式基准**：Stream AC ($\lambda$)。
    *   **批处理基准**：标准的 SAC 和 TD3（作为性能上限参考）。
*   **评估指标**：10 次随机种子实验的平均未折现回报。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **训练规模**：从零开始训练的实验运行了 **2000 万步（20M steps）**；批处理对比实验运行了 300 万步。考虑到流式更新每步仅需一次反向传播且无需维护回放池，其单步计算成本远低于批处理方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在超过 10 个不同的连续控制环境中进行了测试。
    *   每组实验均重复 **10 次随机种子**，提供了置信区间，结果具有统计说服力。
    *   包含针对自适应熵系数和目标噪声的**消融实验**。
*   **充分性评价**：实验设计较为充分，不仅验证了算法从零开始学习的能力，还重点测试了从批处理模型切换到流式微调的实际表现，这在同类研究中较为少见。

### 6. 主要结论与发现
*   **性能对等**：S2AC 和 SDAC 在无需繁琐调参的情况下，达到了与当前最强流式算法 Stream AC ($\lambda$) 相当的性能。
*   **切换障碍**：直接从批处理（Adam 优化器）切换到流式（ObGD 优化器）会导致性能剧烈下降。
*   **优化器偏置**：发现 Adam 优化器会导致 Critic 权重范数过大，降低网络塑性。
*   **解决方案**：在批处理预训练阶段使用 **SGDC（带裁剪的随机梯度下降）** 替代 Adam，可以显著提高后续流式微调的成功率和稳定性。

### 7. 优点
*   **实用性强**：直接解决了 Sim2Real 迁移中边缘设备算力不足的痛点。
*   **架构兼容**：设计的算法与主流 DRL 框架高度一致，方便工业界现有模型的迁移。
*   **鲁棒性**：通过自适应熵缩放解决了流式学习中数据分布漂移导致的探索失效问题。

### 8. 不足与局限
*   **复杂任务挑战**：在极高维或极复杂的任务（如 `quadruped-run`）中，从批处理到流式的切换仍存在明显的性能回退，尚未完全实现“无缝”过渡。
*   **S2AC 微调不稳**：相比于确定性的 SDAC，随机性的 S2AC 在微调阶段对熵系数极其敏感，训练稳定性仍有提升空间。
*   **硬件实测缺失**：虽然论文强调了边缘设备应用，但实验均在仿真器中完成，缺乏在真实嵌入式硬件（如树莓派或微控制器）上的实际功耗和延迟测量。

（完）
