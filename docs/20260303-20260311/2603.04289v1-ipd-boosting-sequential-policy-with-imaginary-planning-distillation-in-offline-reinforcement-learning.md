---
title: "IPD: Boosting Sequential Policy with Imaginary Planning Distillation in Offline Reinforcement Learning"
title_zh: IPD：在离线强化学习中通过虚构规划蒸馏增强序列策略
authors: "Yihao Qin, Yuanfei Wang, Hang Zhou, Peiran Liu, Hao Dong, Yiding Ji"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04289v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 离线强化学习中准最优价值函数的蒸馏
tldr: 针对离线强化学习中基于Transformer的序列策略受限于数据集质量和缺乏规划能力的问题，本文提出IPD框架。该框架利用世界模型和模型预测控制（MPC）生成高质量的想象轨迹来增强离线数据，并通过价值引导的目标函数训练Transformer策略。IPD用学习到的准最优价值函数取代传统的RTG，显著提升了决策稳定性和性能，在D4RL基准测试中超越了现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的序列策略模型难以有效整合次优经验，且缺乏对最优策略的显式规划能力。
method: 通过世界模型和MPC生成可靠的想象最优轨迹来增强数据集，并结合价值引导目标和准最优价值函数训练Transformer。
result: 在D4RL基准测试的多种任务中，IPD的性能显著优于现有的基于价值和基于Transformer的离线强化学习方法。
conclusion: 将离线规划与序列建模相结合，能有效克服离线数据集的局限性，提升Transformer策略在复杂任务中的表现。
---

## 摘要
基于决策 Transformer 的序列策略已成为离线强化学习（RL）中的一种强大范式，但其有效性仍受限于静态数据集的质量和固有的架构限制。具体而言，这些模型通常难以有效整合次优经验，且无法显式地规划最优策略。为了弥补这一差距，我们提出了虚构规划蒸馏（Imaginary Planning Distillation, IPD），这是一个将离线规划无缝融入数据生成、监督训练和在线推理的新颖框架。我们的框架首先从离线数据中学习一个配备不确定性度量的世界模型和一个准最优价值函数。这些组件被用于识别次优轨迹，并利用通过模型预测控制（MPC）生成的可靠、虚构的最优展开（rollouts）对其进行增强。随后，在这一丰富的数据集上训练基于 Transformer 的序列策略，并辅以价值引导的目标函数，以促进最优策略的蒸馏。通过将传统的、手动调节的待获得回报（return-to-go）替换为学习到的准最优价值函数，IPD 提升了推理过程中的决策稳定性和性能。在 D4RL 基准测试上的实证评估表明，IPD 在多种任务中显著优于几种最先进的基于价值和基于 Transformer 的离线强化学习方法。

## Abstract
Decision transformer based sequential policies have emerged as a powerful paradigm in offline reinforcement learning (RL), yet their efficacy remains constrained by the quality of static datasets and inherent architectural limitations. Specifically, these models often struggle to effectively integrate suboptimal experiences and fail to explicitly plan for an optimal policy. To bridge this gap, we propose \textbf{Imaginary Planning Distillation (IPD)}, a novel framework that seamlessly incorporates offline planning into data generation, supervised training, and online inference. Our framework first learns a world model equipped with uncertainty measures and a quasi-optimal value function from the offline data. These components are utilized to identify suboptimal trajectories and augment them with reliable, imagined optimal rollouts generated via Model Predictive Control (MPC). A Transformer-based sequential policy is then trained on this enriched dataset, complemented by a value-guided objective that promotes the distillation of the optimal policy. By replacing the conventional, manually-tuned return-to-go with the learned quasi-optimal value function, IPD improves both decision-making stability and performance during inference. Empirical evaluations on the D4RL benchmark demonstrate that IPD significantly outperforms several state-of-the-art value-based and transformer-based offline RL methods across diverse tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《IPD: Boosting Sequential Policy with Imaginary Planning Distillation in Offline Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：离线强化学习（Offline RL）旨在从固定数据集中学习策略。近年来，基于 Transformer 的序列策略（如 Decision Transformer, DT）因其强大的序列建模能力而受到关注。
*   **核心问题**：
    1.  **缺乏“缝合”能力**：DT 本质上是条件序列模仿，难以将多个次优轨迹中的片段组合（Stitch）成一个最优策略。
    2.  **缺乏显式规划**：DT 依赖于预设的“待获得回报”（Return-to-Go, RTG），无法在训练或推理中进行动态规划。
    3.  **推理不稳定性**：手动调节 RTG 参数不仅耗时，且容易导致决策性能波动。
*   **整体含义**：本文提出 **IPD（虚构规划蒸馏）** 框架，旨在通过将模型预测控制（MPC）和动态规划（DP）的思想融入 Transformer 的训练与推理，克服离线数据集的质量限制。

### 2. 方法论
IPD 框架包含四个关键阶段，将“虚构规划”贯穿始终：
*   **准最优价值学习**：利用改进的 Huber-expectile 回归（基于 IQL 思想）从原始数据中学习准最优的 $V$ 函数和 $Q$ 函数。这种方法对离群值更具鲁棒性，能更好地估计状态的潜在价值。
*   **带不确定性度量的世界模型**：训练一个概率集成世界模型（Ensemble World Model）。创新性地引入了**几何詹森-香农（GJS）散度**来量化模型的不确定性，确保后续的规划仅在模型可靠的区域内进行。
*   **虚构规划数据增强**：
    1.  **识别次优状态**：对比原始轨迹回报与基于世界模型生成的“想象”回报，找出提升潜力最大的状态。
    2.  **MPC 轨迹生成**：在选定的次优状态上，利用 MPC 在世界模型中进行多步规划，生成高质量的虚构最优轨迹，并替换原始数据集中的次优部分。
*   **策略蒸馏与推理**：
    1.  **价值引导目标**：在训练 Transformer 时，除了动作模仿损失，还加入 $Q$ 函数的动作梯度引导，鼓励产生高价值动作。
    2.  **动态 RTG 引导**：在推理阶段，用学习到的准最优 $V(s)$ 动态替代手动设置的 RTG，实现自动化的最优目标引导。

### 3. 实验设计
*   **数据集/场景**：使用离线 RL 标准基准 **D4RL**。
    *   **Gym 任务**：HalfCheetah, Hopper, Walker2d（包含 medium 和 medium-replay 子集）。
    *   **Kitchen 任务**：复杂的长程操作任务。
    *   **Adroit 任务**：稀疏奖励的高维人类演示任务（Hammer, Pen）。
*   **对比方法（Baselines）**：
    *   **传统价值方法**：CQL, IQL。
    *   **Transformer 变体**：DT, QDT, EDT, QT, Reinformer。
    *   **扩散模型方法**：Decision Diffuser (DD)。

### 4. 资源与算力
*   **硬件**：论文明确指出所有实验均在 **NVIDIA GeForce RTX 4090 GPU** 上进行。
*   **软件细节**：使用了 Adam 优化器，学习率为 3e-4。Transformer 采用 4 层架构，嵌入维度 256，注意力头数为 4。
*   **训练时长**：文中未明确给出每个任务的具体训练总时长，但详细列出了各组件的超参数设置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大类场景共 10 个核心任务。每个任务均进行了 **10 次评估（Tenfold evaluation）** 以确保结果的统计显著性。
*   **消融实验**：
    *   对比了 MPC 增强与普通贪婪 Q 学习增强的效果。
    *   分析了数据增强规模（Augmentation Ratio）对性能的影响，揭示了**缩放法则（Scaling Law）**。
    *   验证了准最优价值函数相比手动 RTG 在推理稳定性上的优势。
*   **充分性评价**：实验设计全面，不仅对比了 SOTA 方法，还深入探讨了每个模块的贡献，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：IPD 在几乎所有 D4RL 任务中均优于现有的基于价值和基于 Transformer 的方法，尤其在次优数据（medium-replay）上提升显著。
*   **缩放效应**：发现随着虚构数据生成量的增加，策略性能呈现近似线性的提升趋势。
*   **稳定性提升**：通过学习到的 $V(s)$ 进行引导，消除了 DT 对 RTG 调参的依赖，显著降低了推理时的性能方差。

### 7. 优点
*   **架构创新**：成功将基于模型的规划（MPC）与基于序列的模仿（Transformer）无缝结合，解决了 DT 的“缝合”难题。
*   **可靠性高**：引入不确定性度量（GJS 散度）来约束虚构规划，有效防止了模型误差导致的性能崩溃。
*   **易用性**：动态价值引导使得模型在实际部署时更加自动化，无需人工干预目标值。

### 8. 不足与局限
*   **计算开销**：相比于纯无模型方法，IPD 需要额外训练世界模型和价值函数，且 MPC 采样过程会增加数据预处理的计算负担。
*   **模型依赖性**：策略的上限受限于世界模型的建模精度。在环境动力学极其复杂或高度随机的情况下，虚构规划的质量可能会下降。
*   **任务覆盖**：虽然在 D4RL 表现优异，但尚未在更大规模的真实世界机器人任务或视觉输入任务中进行验证。

（完）
