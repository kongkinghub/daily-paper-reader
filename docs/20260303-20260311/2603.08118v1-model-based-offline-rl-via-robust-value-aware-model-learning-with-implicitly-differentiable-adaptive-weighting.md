---
title: Model-based Offline RL via Robust Value-Aware Model Learning with Implicitly Differentiable Adaptive Weighting
title_zh: 基于隐式可微自适应加权的鲁棒价值感知模型学习的基于模型的离线强化学习
authors: "Zhongjian Qiao, Jiafei Lyu, Boxiang Lyu, Yao Shu, Siyang Gao, Shuang Qiu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08118v1"
tags: ["query:rl-ts"]
score: 10.0
evidence: 揭示并解决基于模型的离线强化学习中严重的 Q 值低估问题
tldr: 针对离线强化学习中模型利用过度导致的性能下降问题，本文提出了ROMI框架。ROMI通过鲁棒价值感知模型学习，在可调的状态不确定性集合内预测最小Q值，实现了可控的保守性并解决了RAMBO算法中的梯度爆炸和Q值低估问题。此外，引入隐式可微自适应加权的双层优化方案，增强了模型在多步展开中对分布外数据的泛化能力。实验证明ROMI在D4RL和NeoRL数据集上显著优于RAMBO并达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的对抗性模型学习方法如RAMBO在超参数微调下易出现Q值严重低估和梯度爆炸，导致模型更新不稳定且过于保守。
method: 提出ROMI框架，通过在状态不确定性集合内进行鲁棒价值感知学习，并结合隐式可微自适应加权的双层优化机制来平衡动力学与价值感知。
result: 实验结果显示ROMI在D4RL和NeoRL数据集上表现优异，不仅解决了RAMBO的局限性，还达到了与当前顶尖方法相当或更优的性能。
conclusion: ROMI通过提供可控的保守性和稳定的模型更新机制，为解决离线强化学习中的模型误差和分布偏移问题提供了一种有效的鲁棒学习方案。
---

## 摘要
基于模型的离线强化学习（RL）旨在通过动力学模型增强离线强化学习，从而促进策略探索。然而，由于不可避免的模型误差，可能会发生模型利用（model exploitation），从而降低算法性能。对抗性模型学习通过解决极大极小（maximin）公式，提供了一个缓解模型利用的理论框架。在这种范式中，RAMBO 是一种具有代表性且最流行的方法，它提供了基于模型梯度的实际实现。然而，我们通过实验发现，RAMBO 在仅进行轻微超参数调整时就可能出现严重的 Q 值低估和梯度爆炸，这表明它往往过于保守，且模型更新不稳定。为了解决这些问题，我们提出了基于隐式可微自适应加权的鲁棒价值感知模型学习（ROMI）。ROMI 引入了一种新型的鲁棒价值感知模型学习方法，而不是使用模型梯度来更新动力学模型。该方法要求动力学模型预测的未来状态价值接近尺度可调的状态不确定性集内的最小 Q 值，从而实现可控的保守性和稳定的模型更新。为了进一步提高多步展开（rollouts）过程中的分布外（OOD）泛化能力，我们提出了隐式可微自适应加权，这是一种自适应实现动力学感知和价值感知模型学习的双层优化方案。在 D4RL 和 NeoRL 数据集上的实验结果表明，ROMI 显著优于 RAMBO，并在 RAMBO 通常表现不佳的数据集上达到了与其他最先进方法相当或更优的性能。代码可在 https://github.com/zq2r/ROMI.git 获取。

## Abstract
Model-based offline reinforcement learning (RL) aims to enhance offline RL with a dynamics model that facilitates policy exploration. However, \textit{model exploitation} could occur due to inevitable model errors, degrading algorithm performance. Adversarial model learning offers a theoretical framework to mitigate model exploitation by solving a maximin formulation. Within such a paradigm, RAMBO~\citep{rigter2022rambo} has emerged as a representative and most popular method that provides a practical implementation with model gradient. However, we empirically reveal that severe Q-value underestimation and gradient explosion can occur in RAMBO with only slight hyperparameter tuning, suggesting that it tends to be overly conservative and suffers from unstable model updates. To address these issues, we propose \textbf{RO}bust value-aware \textbf{M}odel learning with \textbf{I}mplicitly differentiable adaptive weighting (ROMI). Instead of updating the dynamics model with model gradient, ROMI introduces a novel robust value-aware model learning approach. This approach requires the dynamics model to predict future states with values close to the minimum Q-value within a scale-adjustable state uncertainty set, enabling controllable conservatism and stable model updates. To further improve out-of-distribution (OOD) generalization during multi-step rollouts, we propose implicitly differentiable adaptive weighting, a bi-level optimization scheme that adaptively achieves dynamics- and value-aware model learning. Empirical results on D4RL and NeoRL datasets show that ROMI significantly outperforms RAMBO and achieves competitive or superior performance compared to other state-of-the-art methods on datasets where RAMBO typically underperforms. Code is available at https://github.com/zq2r/ROMI.git.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model-based Offline RL via Robust Value-Aware Model Learning with Implicitly Differentiable Adaptive Weighting》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在基于模型的离线强化学习（Offline RL）中，策略容易利用动力学模型的预测误差（即“模型利用”问题），导致在真实环境中表现不佳。
*   **研究动机**：现有的主流对抗性模型学习方法（如 RAMBO）虽然试图通过最小化 OOD（分布外）区域的价值来引入保守性，但存在两个严重缺陷：
    1.  **保守性难以控制**：对超参数 $\lambda$ 极度敏感，轻微调整即可导致 Q 值严重低估。
    2.  **训练不稳定**：容易出现梯度爆炸，导致训练崩溃。
*   **整体含义**：本文旨在提出一种更稳定、保守性可控且泛化能力更强的模型学习框架，以解决离线 RL 中的模型利用难题。

### 2. 论文提出的方法论：ROMI
ROMI 框架由两个核心组件组成：
*   **鲁棒价值感知模型学习（RVL）**：
    *   **核心思想**：放弃 RAMBO 的模型梯度更新方式，转而要求动力学模型预测的未来状态价值，应接近于以当前预测为中心、半径为 $\xi$ 的“状态不确定性集”内的最小 Q 值。
    *   **技术细节**：利用 Wasserstein 距离的对偶性质，将复杂的动力学不确定性集转化为易于处理的状态扰动集。通过调整扰动半径 $\xi$，可以精确且直观地控制保守程度。
*   **隐式可微自适应加权（Implicitly Differentiable Adaptive Weighting）**：
    *   **核心思想**：采用双层优化（Bi-level Optimization）机制，平衡“动力学感知”（模型预测要准）和“价值感知”（模型要保守）。
    *   **算法流程**：
        *   **内层优化**：通过一个自适应加权网络 $w_\nu$ 对训练样本进行加权，执行加权监督学习（WSL），提升模型对环境动力学的重建能力。
        *   **外层优化**：利用隐式微分（Implicit Differentiation）更新加权网络参数 $\nu$，目标是最小化 RVL 损失。这使得模型能自适应地识别并重视那些对降低价值误差关键的样本。

### 3. 实验设计
*   **数据集与场景**：
    *   **D4RL 标准基准**：包含 12 个 MuJoCo 任务（HalfCheetah, Hopper, Walker2d 的不同质量数据）和 6 个挑战性的 Antmaze 任务。
    *   **NeoRL 基准**：包含 9 个更接近现实场景的任务，数据分布更窄。
*   **对比方法（Baselines）**：
    *   **无模型方法**：CQL, IQL, EDAC。
    *   **有模型方法**：MOPO, RAMBO（直接竞争对手）, Count-MORL, MOBILE。
*   **消融实验**：验证了自适应加权对降低多步展开误差的作用，以及超参数 $\xi$ 对 Q 值和训练稳定性的影响。

### 4. 资源与算力
*   **硬件设施**：论文明确指出使用了 **4 张 NVIDIA RTX A6000 GPU** 和 AMD Ryzen Threadripper PRO 5975WX CPU。
*   **训练时长**：在附录 E.2 中提供了运行时间对比。ROMI 的运行速度略慢于 RAMBO（例如在 Hopper 任务中，ROMI 约为 28.2s/epoch，而 RAMBO 约为 23.7s/epoch），这是由于双层优化中加权网络的额外计算开销所致。
*   **参数规模**：模型参数量约为 8MB 左右，GPU 显存占用约 3.3GB。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个大类基准（D4RL MuJoCo, D4RL Antmaze, NeoRL）共 27 个子数据集上进行了测试。
*   **充分性**：
    *   每个实验均使用 **5 个随机种子** 取平均值，并提供了标准差。
    *   针对 RAMBO 的局限性做了深入的动机实验（9 个数据集上的梯度和 Q 值分析）。
    *   **公平性**：所有方法统一在 1M 训练步数下对比，且使用了相同的开源框架（OfflineRL-Kit）实现，确保了对比的客观性。

### 6. 主要结论与发现
*   **性能卓越**：ROMI 在 D4RL MuJoCo 上比 RAMBO 总分提升了 18.6%，在 NeoRL 和 Antmaze 任务上也均达到了 SOTA 或同等水平。
*   **稳定性显著提升**：实验证明 ROMI 在大范围的 $\xi$ 取值下均能保持稳定的梯度和 Q 值估计，彻底解决了 RAMBO 的梯度爆炸问题。
*   **泛化能力强**：通过自适应加权，模型在多步展开（Rollout）中的预测误差显著低于不加权的版本，证明了动力学感知的重要性。

### 7. 优点（亮点）
*   **理论与实践结合**：基于 Wasserstein 距离的对偶推导为状态不确定性集提供了坚实的理论支撑。
*   **保守性可控**：将抽象的拉格朗日乘子 $\lambda$ 替换为具有物理意义的扰动半径 $\xi$，使算法更易于调优。
*   **双层优化创新**：引入隐式微分来动态调整样本权重，巧妙地解决了模型准确性与保守性之间的权衡。

### 8. 不足与局限
*   **计算开销**：双层优化结构增加了训练时间，虽然显存占用不高，但在大规模任务上的效率仍有优化空间。
*   **超参数预设**：保守性参数 $\xi$ 仍需在训练前指定，无法像某些自适应方法那样在运行时根据模型反馈动态调整。
*   **复杂度**：相比于简单的惩罚项方法（如 MOPO），ROMI 的实现逻辑（隐式微分、加权网络）相对复杂，增加了部署门槛。

（完）
