---
title: Robust Regularized Policy Iteration under Transition Uncertainty
title_zh: 转移不确定性下的鲁棒正则化策略迭代
authors: "Hongqiang Lin, Zhenghui Fu, Weihao Tang, Pengfei Wang, Yiding Sun, Qixian Huang, Dongxu Zhang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09344v1"
tags: ["query:rl-ts"]
score: 9.0
evidence: 解决离线强化学习中策略引起的外推和转移不确定性
tldr: 针对离线强化学习中分布偏移和转移不确定性导致性能下降的问题，本文提出了一种鲁棒正则化策略迭代（RRPI）框架。该方法将转移核视为不确定集中的决策变量，通过优化最坏情况下的动力学来提升策略鲁棒性。RRPI利用KL正则化代理将复杂的双层优化转化为可处理的形式，并证明了其收敛性。实验表明，RRPI在D4RL基准测试中优于现有基准，能有效规避高不确定性区域。
selection_source: fresh_fetch
motivation: 离线强化学习在面对分布偏移和不可靠的转移模型时，容易因访问分布外状态而导致性能大幅下降。
method: 提出RRPI算法，通过引入KL正则化代理将鲁棒策略优化转化为可处理的策略迭代过程，并利用鲁棒正则化贝尔曼算子进行更新。
result: 在D4RL基准测试中，RRPI在多数环境下优于PMDB等最新基准，且在不确定性较高的区域表现出更保守的Q值估计。
conclusion: RRPI通过统一处理策略诱导的外推和转移不确定性，为构建稳健且高效的离线强化学习算法提供了理论支撑和实践方案。
---

## 摘要
离线强化学习（Offline RL）能够在无需在线探索的情况下实现高效且安全的策略学习，但其性能往往在分布偏移下有所下降。所学习的策略可能会访问分布外（out-of-distribution）的状态-动作对，而在这些区域，价值估计和学习到的动力学模型是不可靠的。为了在一个统一的框架内解决策略诱导的外推和转移不确定性，我们将离线强化学习建模为鲁棒策略优化，将转移核视为不确定性集合中的决策变量，并针对最坏情况下的动力学优化策略。我们提出了鲁棒正则化策略迭代（RRPI），它将难以处理的极大极小双层目标替换为可处理的 KL 正则化代理目标，并基于鲁棒正则化贝尔曼算子推导出了高效的策略迭代过程。我们提供了理论保证，证明了所提出的算子是一个 γ-收缩映射，并且迭代更新代理目标能够使原始鲁棒目标单调提升并收敛。在 D4RL 基准测试上的实验表明，RRPI 取得了强大的平均性能，在大多数环境中优于包括基于分位数的 PMDB 在内的近期基准方法，并在其余环境中保持了竞争力。此外，RRPI 表现出了鲁棒行为。在认知不确定性较高的区域，学习到的 Q 值会降低，这表明所得策略在转移不确定性下会避开不可靠的分布外动作。

## Abstract
Offline reinforcement learning (RL) enables data-efficient and safe policy learning without online exploration, but its performance often degrades under distribution shift. The learned policy may visit out-of-distribution state-action pairs where value estimates and learned dynamics are unreliable. To address policy-induced extrapolation and transition uncertainty in a unified framework, we formulate offline RL as robust policy optimization, treating the transition kernel as a decision variable within an uncertainty set and optimizing the policy against the worst-case dynamics. We propose Robust Regularized Policy Iteration (RRPI), which replaces the intractable max-min bilevel objective with a tractable KL-regularized surrogate and derives an efficient policy iteration procedure based on a robust regularized Bellman operator. We provide theoretical guarantees by showing that the proposed operator is a $γ$-contraction and that iteratively updating the surrogate yields monotonic improvement of the original robust objective with convergence. Experiments on D4RL benchmarks demonstrate that RRPI achieves strong average performance, outperforming recent baselines including percentile-based methods such as PMDB on the majority of environments while remaining competitive on the rest. Moreover, RRPI exhibits robust behavior. The learned $Q$-values decrease in regions with higher epistemic uncertainty, suggesting that the resulting policy avoids unreliable out-of-distribution actions under transition uncertainty.

---

## 论文详细总结（自动生成）

这篇论文《Robust Regularized Policy Iteration under Transition Uncertainty》提出了一种名为 **RRPI** 的新框架，旨在解决离线强化学习（Offline RL）中由于数据覆盖不足导致的动力学模型不确定性和分布偏移问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
离线强化学习的核心挑战在于**分布偏移**：学习到的策略可能会选择训练数据中未覆盖的动作，导致价值估计崩溃。
*   **现有局限**：传统方法通常采用保守的价值学习或策略约束，但往往过于保守，且大多基于单一的动力学模型，忽略了环境转移过程本身的不确定性。
*   **研究动机**：将离线 RL 建模为**鲁棒策略优化**问题。通过将转移核（Transition Kernel）视为不确定集中的变量，让策略在“最坏情况”的动力学下进行优化，从而在不依赖启发式惩罚的情况下，从根本上提高策略的稳健性。

### 2. 方法论：核心思想与技术细节
RRPI 的核心是将难以处理的极大极小（Max-Min）双层优化问题转化为可处理的迭代过程：
*   **核心思想**：引入一个 **KL 正则化代理目标**。通过在目标函数中加入策略与参考策略之间的 KL 散度正则项，将原始的鲁棒优化问题转化为一种类似于“软”策略迭代的形式。
*   **关键技术细节**：
    *   **鲁棒正则化贝尔曼算子**：定义了一个新的算子 $T$，在计算目标值时，不仅考虑奖励，还会在不确定集 $P$ 中搜索使未来价值最小化的转移概率。
    *   **不确定集构建**：在实践中，使用**动力学模型集成（Model Ensemble）**来近似不确定集。在贝尔曼回溯时，从集成模型中选择产生最小目标值的模型作为“最坏情况”的代理。
    *   **策略更新**：策略通过最小化与玻尔兹曼分布（由 Q 函数导出）之间的 KL 散度进行更新，从而实现平滑的策略改进。
*   **理论保证**：论文证明了该算子是 $\gamma$-收缩映射，保证了收敛到唯一固定点；同时证明了迭代更新代理目标能导致原始鲁棒目标的单调提升。

### 3. 实验设计
*   **数据集/场景**：使用了标准的 **D4RL 基准测试**，涵盖了 MuJoCo 任务（HalfCheetah, Hopper, Walker2d）的多种数据配置（Random, Medium, Expert, Medium-Expert, Medium-Replay, Full-Replay）。
*   **对比方法（Baselines）**：
    *   **无模型方法**：CQL, DMG, EPQ。
    *   **有模型方法**：MOReL, RAMBO, PMDB（近期最强基准之一）, AMG, ADM。
*   **评估指标**：归一化得分（Normalized Score）的平均值和标准差。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实验设置**：提到结果是基于 **4 个随机种子**的平均值。

### 5. 实验数量与充分性
*   **实验规模**：在 18 个不同的 D4RL 任务上进行了全面测试，实验覆盖面广。
*   **消融实验**：设计了针对“最坏情况模型选择”的消融实验，验证了鲁棒优化机制对性能提升的必要性。
*   **不确定性分析**：通过可视化 Q 值与模型预测不确定性（标准差）的关系，展示了算法如何自动避开高风险区域。
*   **评价**：实验设计较为充分且客观，对比了多种类型的最新算法，能够有力支撑其理论贡献。

### 6. 主要结论与发现
*   **性能领先**：RRPI 在 D4RL 的 18 个环境中，有 11 个优于目前最先进的 PMDB 算法，并在其余环境中保持了竞争力。
*   **隐式保守性**：虽然 RRPI 没有显式的显式价值惩罚项，但其鲁棒优化机制会在认知不确定性高的区域自动降低 Q 值估计，使策略表现出自然的保守性。
*   **稳定性**：正则化项有效防止了策略在训练过程中的剧烈波动，提高了离线训练的稳定性。

### 7. 优点：亮点与创新
*   **理论与实践结合**：不仅提出了实用的算法，还提供了严谨的收敛性和单调改进证明。
*   **统一框架**：在一个鲁棒优化的框架下同时处理了外推误差和动力学不确定性，比启发式方法更具原则性。
*   **无需显式不确定性估计**：通过模型集成的最坏情况搜索，避免了复杂的显式不确定性量化计算。

### 8. 不足与局限
*   **计算开销**：虽然比直接解双层优化快，但由于需要维护和查询模型集成（Ensemble），其计算成本仍高于单模型或无模型方法。
*   **不确定集近似**：实际实现中对不确定集 $P$ 的近似（仅从有限的集成模型中选最小）可能无法完全覆盖真实的最坏情况。
*   **任务局限性**：实验主要集中在连续控制任务（MuJoCo），尚未在视觉输入或更复杂的长程逻辑推理任务中验证其泛化能力。

（完）
